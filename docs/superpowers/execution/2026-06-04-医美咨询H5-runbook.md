# 医美咨询 H5 网页 — 实施 Runbook

> **本文件用途**：项目框架 + 38 个任务的执行卡 + 进度跟踪。
> 配合 `docs/superpowers/specs/2026-06-04-医美咨询H5-design.md`（设计文档 v2.0）使用。
> 配合 `docs/superpowers/plans/2026-06-04-医美咨询H5-implementation.md`（实施计划）使用。
> 上一版 `2026-06-04-医美咨询小程序-runbook.md` 已废弃（v1.0 云函数/小程序架构）。

---

## 0. 项目框架

### 0.1 一句话定位

**H5 网页**模式，给医美咨询师在手机/平板上上传客户照片 + 选预设项目 + AI 生成整形预览图，电脑端也能用。技术栈：Vue 3 + Vite + Vant（前端）/ NestJS + TypeScript + TypeORM（后端）/ PostgreSQL + Redis + Bull（数据与队列），无算法备案、无主体资质要求。

### 0.2 目录结构（target state）

```
/Users/chenjin/Downloads/beautify_work/
├── web/                              # 用户端 H5（Vue 3 + Vite + Vant）
│   ├── src/
│   │   ├── pages/                    # 11 个页面
│   │   ├── components/               # 共享组件
│   │   ├── stores/                   # Pinia stores
│   │   ├── api/                      # Axios 封装 + 各模块 API
│   │   ├── router.js
│   │   ├── main.js
│   │   ├── App.vue
│   │   └── styles/                   # 全局样式 + 响应式 mixin
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── admin/                            # 运营后台 H5（Vue 3 + Vite + Element Plus）
│   ├── src/
│   │   ├── pages/                    # 9 个页面（dashboard/orders/users/...）
│   │   ├── components/
│   │   ├── stores/
│   │   ├── api/
│   │   ├── router.js
│   │   ├── main.js
│   │   └── App.vue
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── server/                           # NestJS 后端
│   ├── src/
│   │   ├── modules/                  # 业务模块
│   │   │   ├── auth/                 # 短信 + JWT
│   │   │   ├── user/                 # 用户资料 + 注销
│   │   │   ├── credit/               # 积分事务
│   │   │   ├── order/                # 订单
│   │   │   ├── payment/              # 微信/支付宝 H5
│   │   │   ├── agreement/            # 协议
│   │   │   ├── preset/               # 预设项目
│   │   │   ├── generate/             # 提交 + 队列 + worker
│   │   │   ├── ai/                   # AIService（通义/混元）
│   │   │   ├── notification/         # 站内通知
│   │   │   └── admin/                # 运营后台接口
│   │   ├── common/                   # 公共
│   │   │   ├── guards/               # JwtAuthGuard / AdminAuthGuard / RolesGuard
│   │   │   ├── pipes/                # ValidationPipe / ZodValidationPipe
│   │   │   ├── filters/              # HttpExceptionFilter
│   │   │   ├── interceptors/         # TransformInterceptor / LoggingInterceptor
│   │   │   ├── decorators/           # @CurrentUser / @Roles / @Public
│   │   │   └── middleware/           # 限流 / 请求 ID
│   │   ├── entities/                 # TypeORM 实体（14 张表）
│   │   ├── migrations/               # TypeORM 迁移
│   │   ├── config/                   # 配置（database / redis / jwt / oss / ...）
│   │   ├── utils/                    # crypto / logger / sms / oss / wechatpay / alipay
│   │   ├── queues/                   # Bull 队列定义
│   │   ├── workers/                  # Bull worker 进程入口
│   │   ├── cron/                     # 定时任务
│   │   ├── main.ts
│   │   └── app.module.ts
│   ├── test/                         # e2e（supertest）
│   ├── package.json
│   ├── tsconfig.json
│   ├── tsconfig.build.json
│   ├── nest-cli.json
│   └── ormconfig.ts
│
├── shared/                           # 前后端共享
│   ├── types/                        # TypeScript 类型
│   └── constants/                    # 业务常量
│
├── tests/                            # 跨项目测试
│   ├── unit/                         # 跨模块单元测试
│   ├── integration/                  # 集成测试（需真实 DB）
│   └── e2e/                          # Playwright 端到端
│
├── docs/
│   ├── superpowers/
│   │   ├── specs/                    # 设计终稿
│   │   ├── plans/                    # 实施计划
│   │   └── execution/                # 本 Runbook
│   └── 医美咨询小程序-设计草稿-2026-06-04.md  # 已废弃
│
├── docker-compose.yml                # 本地开发：PG + Redis
├── docker-compose.prod.yml           # 生产：web + admin + server + worker + nginx + pg + redis
├── .github/workflows/                # CI/CD
│   ├── ci.yml                        # lint + test
│   ├── build.yml                     # docker build
│   └── deploy.yml                    # 部署到阿里云 ECS
│
├── package.json                      # 根（workspaces: web, admin, server, shared）
├── tsconfig.base.json
├── .gitignore
├── .editorconfig
├── .nvmrc                            # Node 20+
├── README.md
└── .env.example                      # 环境变量模板（不提交真实值）
```

### 0.3 技术栈

| 层 | 技术 |
|---|---|
| 用户端前端 | Vue 3 + Vite + Vant 4 + Pinia + Vue Router 4 + Axios |
| 后台前端 | Vue 3 + Vite + Element Plus + Pinia + Vue Router 4 + Axios |
| 后端 | NestJS 10 + TypeScript 5 + TypeORM 0.3 |
| 数据库 | PostgreSQL 15+ |
| 缓存/队列 | Redis 7 + Bull 4 |
| 文件存储 | 阿里云 OSS（生产）/ 本地（开发）|
| AI 厂商 | 阿里通义万相 image editing（主）+ 腾讯混元（备）|
| 支付 | 微信 H5 支付 v3 + 支付宝手机网站支付 |
| 短信 | 阿里云短信 |
| 图片处理 | sharp（AI 角标合成）|
| 单元/集成测试 | Jest + ts-jest + supertest |
| E2E 测试 | Playwright |
| 监控 | @willsoto/nestjs-prometheus + prom-client + Grafana |
| 告警 | 企业微信群机器人 webhook |
| 日志 | nestjs-pino + pino-pretty（开发）|
| CI/CD | GitHub Actions |
| 部署 | 阿里云 ECS + Docker Compose + Nginx |

### 0.4 执行约定（**所有任务都遵守**）

1. **TDD 节奏**：写测试 → 跑测试看到失败 → 实现 → 跑测试看到通过 → commit
2. **提交格式**：`<type>(<scope>): <description>`
   - `type` ∈ {feat, fix, refactor, test, docs, chore, style}
   - `scope` 是模块名：`auth`, `user`, `credit`, `payment`, `ai`, `generate`, `admin`, `shared`, `web`, `server`, `webapp`...
   - 例：`feat(auth): 短信验证码登录 + JWT`、`fix(ai): 主模型 5xx 退积分时漏写 audit_log`
3. **绝对路径**：所有文件操作都用 `/Users/chenjin/Downloads/beautify_work/...` 绝对路径
4. **分支**：`feature/h5-redesign`（当前活跃分支）
5. **测试命令**：
   - 单元：`npm run test:unit`
   - 集成：`npm run test:integration`（需本地 Docker PG/Redis）
   - E2E：`npm run test:e2e`（需 Playwright + 本地 dev server）
   - 全部：`npm test`
6. **代码组织**：
   - 单文件单一职责
   - 业务/控制器/服务/仓储分离（NestJS 模块化）
   - 共享代码放 `server/src/common/` 或 `shared/`
7. **deferred 标记**：任务中标 **(deferred)** 的步骤需真实环境（阿里云账号 / 微信商户号 / 真实手机号 / 真实 OSS / 真实域名），本地不执行，标记 ⏸ pending-deployment
8. **环境变量**：所有 secret 走 `process.env`，在 `server/src/config/` 集中加载，配 `.env.example` 模板。**不提交真实值到 git**。

### 0.5 真实环境前置（陈工负责，本 Runbook 不执行）

在动手前，陈工需要：

- [ ] 注册阿里云账号，开通 **短信服务** + **OSS** + **ECS**
- [ ] 申请**微信 H5 支付**商户号（公司主体）
- [ ] 申请**支付宝手机网站支付**（公司主体）
- [ ] 注册**阿里云**账号，签约**通义万相** image editing 服务
- [ ] 注册**腾讯云**账号，签约**混元** image editing 服务
- [ ] 购买**域名** + **SSL 证书**（生产）
- [ ] 安装 **Docker Desktop**（本地开发）
- [ ] 安装 **Node.js 20+** + **pnpm**（推荐，npm 也可）

环境变量（在 `server/.env`，本地可写入；生产在 ECS 环境变量中配）：

```bash
# 基础
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174

# 数据库
DATABASE_URL=postgresql://app:app@localhost:5432/medical_aesthetics

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=30m
JWT_REFRESH_SECRET=change-me-too
JWT_REFRESH_EXPIRES_IN=7d

# 字段加密（AES）
FIELD_ENCRYPT_KEY=32-byte-random-base64

# 短信
SMS_ACCESS_KEY_ID=
SMS_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_CODE=

# AI
TONGYI_API_KEY=
HUNYUAN_SECRET_ID=
HUNYUAN_SECRET_KEY=

# OSS
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=
OSS_REGION=oss-cn-hangzhou

# 微信 H5 支付 v3
WECHATPAY_MCH_ID=
WECHATPAY_API_V3_KEY=
WECHATPAY_NOTIFY_URL=
WECHATPAY_APPID=
WECHATPAY_PRIVATE_KEY_PATH=
WECHATPAY_SERIAL_NO=

# 支付宝
ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY=
ALIPAY_PUBLIC_KEY=
ALIPAY_NOTIFY_URL=

# 监控
WECHAT_BOT_WEBHOOK=
```

### 0.6 阶段划分

| 阶段 | 周 | Task | 内容 |
|---|---|---|---|
| **P0 基础** | W1 | 1-8 | 仓库、Docker、NestJS、TypeORM、Migration、配置日志、公共模块、Web/Admin 脚手架、P0 验收 |
| **P1 用户+协议** | W2 | 9-13 | SMS、登录+JWT、用户资料+注销、协议、P1 验收 |
| **P2 积分+支付** | W3 | 14-19 | 积分事务、套餐+订单、微信/支付宝 H5 支付、回调对账、P2 验收 |
| **P3 预设+AI** | W4-W5 | 20-27 | 预设项目、提示词、适配器、AIService、限流通知、Generate submit+worker、辅助接口、P3 验收 |
| **P4 咨询师端页面** | W6-W7 | 28-31 | 11 个 Vue 页面、P4 验收 |
| **P5 运营后台** | W8-W9 | 32-35 | 后台登录、9 个模块、P5 验收 |
| **P6 收尾** | W10 | 36-38 | 监控告警、E2E+CI/CD、部署发布 |

### 0.7 关键决策（与 design.md D1-D14 对齐）

| # | 决策 |
|---|---|
| D1 | 客户不参与；咨询师专用 ✓ |
| D2 | 收费：按次积分（4 档套餐 50/220/580/2400 积分，¥29/99/229/899）✓ |
| D3 | AI 交互：预设项目 + 可选描述（20 项标准库）✓ |
| D4 | AI 模型：国内（通义主+混元备）✓ |
| D5 | 账号交付：全自助 ✓ |
| D6 | 隐私保留：30 天过期 + 加密 + 手动删 + 审计 ✓ |
| D7 | 异步流程：Bull 队列触发 worker + 站内通知 ✓ |
| D8 | **可运营调整**：任何可变内容都存 DB，运营后台 CRUD，不硬编码 ✓ |
| D9 | **登录**：手机号 + 短信验证码 + JWT（access + refresh） |
| D10 | **支付**：微信 H5 支付 + 支付宝手机网站支付（双通道） |
| D11 | **不需要**：算法备案、个体工商户资质（仅 H5 网页） |
| D12 | **通知**：站内通知表 + 轮询（前端每 5s 拉一次 /api/notification/unread-count） |
| D13 | **文件存储**：阿里云 OSS（生产），本地（开发）|
| D14 | **响应式**：移动端（<768px）+ 平板（768-1024px）+ PC（>1024px）三端自适应 |

---

## 1. 执行进度总览

- [x] **Task 1: 初始化项目仓库** ✅ DONE (commit `6f5b598`)
- [x] **Task 2: Docker Compose + 文档** ✅ DONE (commit `921245f` / README `abba3a0`)
- [x] **Task 3: NestJS 脚手架** ✅ DONE (commit `d000701`)
- [x] **Task 4: TypeORM + Migration 框架** ✅ DONE (commit `0b9b364`)
- [x] **Task 5: 公共模块** ✅ DONE (commit `7fdd112`)
- [x] **Task 6: Web 端 H5 脚手架** ✅ DONE (commit `aa3532d`)
- [x] **Task 7: Admin H5 脚手架** ✅ DONE (commit `3faa762`)
- [x] **Task 8: P0 验收** ✅ DONE (tag `p0-foundation`)
- [x] **Task 9: SMS 模块** ✅ DONE (commit `9c4604c`)
- [x] **Task 10: 登录 + JWT** ✅ DONE (commit `bc09391`)
- [x] **Task 11: 用户资料 + 注销** ✅ DONE (commit `8b10e8f`)
- [x] **Task 12: 协议模块** ✅ DONE (commit `9a507a2`)
- [x] **Task 13: P1 验收** ✅ DONE (commit `1d5ab77`)
- [x] **Task 14: 积分事务（TDD）** ✅ DONE (commit `fb63322`)
- [ ] Task 5: 公共模块
- [ ] Task 6: Web 端 H5 脚手架
- [ ] Task 7: Admin H5 脚手架
- [ ] Task 8: P0 验收
- [ ] Task 9-13: P1 用户+协议
- [ ] Task 14-19: P2 积分+支付
- [ ] Task 20-27: P3 预设+AI
- [ ] Task 28-31: P4 咨询师端页面
- [ ] Task 32-35: P5 运营后台
- [ ] Task 36-38: P6 收尾

进度统计：**11/38 完成**（28.9%）

---

## 2. 任务卡

### ✅ Task 1: 初始化项目仓库 【DONE】

**Goal**: 创建项目根目录的 5 个配置文件 + 首次 commit

**Pre-reqs**: 无

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/.gitignore`
- Create: `/Users/chenjin/Downloads/beautify_work/package.json`
- Create: `/Users/chenjin/Downloads/beautify_work/README.md`
- Create: `/Users/chenjin/Downloads/beautify_work/.editorconfig`
- Create: `/Users/chenjin/Downloads/beautify_work/.nvmrc`

**Steps**: 见 `plan.md` Task 1（2026-06-04 写）

**Test**: `git log --oneline` 应显示 1 个 commit (`6f5b598`)

**Commit**: `chore: 初始化项目结构`

**Done criteria**:
- 5 个文件存在
- `git log` 显示 commit `6f5b598`
- `git status` 干净

**实际 commit**: `6f5b598d88bcfb14fbf2964cdcc80ae3b01a4700`

---

### ✅ Task 2: Docker Compose + 文档 【DONE】

**Goal**: 起一个本地的 PostgreSQL 15 + Redis 7 容器，配置健康检查 + 数据卷持久化

**Pre-reqs**: Task 1 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/docker-compose.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/docker-compose.prod.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/.env.example`
- Create: `/Users/chenjin/Downloads/beautify_work/server/.dockerignore`
- Modify: `/Users/chenjin/Downloads/beautify_work/README.md`

**Steps**:

1. **写 `docker-compose.yml`**（开发用）：

```yaml
version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ma-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: medical_aesthetics
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d medical_aesthetics"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ma-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
```

2. **写 `.env.example`**（提交到 git，真实 `.env` 不提交）：

```bash
NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://app:app@localhost:5432/medical_aesthetics
REDIS_URL=redis://localhost:6379
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=30m
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_REFRESH_EXPIRES_IN=7d
FIELD_ENCRYPT_KEY=dev-key-32-bytes-base64-encoded==
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
# 以下是真实部署才需要的，本地留空
SMS_ACCESS_KEY_ID=
SMS_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_CODE=
TONGYI_API_KEY=
HUNYUAN_SECRET_ID=
HUNYUAN_SECRET_KEY=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=
OSS_REGION=oss-cn-hangzhou
WECHATPAY_MCH_ID=
WECHATPAY_API_V3_KEY=
WECHATPAY_NOTIFY_URL=
WECHATPAY_APPID=
ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY=
ALIPAY_PUBLIC_KEY=
ALIPAY_NOTIFY_URL=
WECHAT_BOT_WEBHOOK=
```

3. **写 `scripts/init.sql`**（初始化时创建扩展）：

```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
```

4. **修改 `.gitignore`** 加入：`.env`、`.env.local`、`.env.production`、`server/dist/`、`web/dist/`、`admin/dist/`、`*.log`

5. **(deferred)** `docker-compose.prod.yml` 写完整生产配置（含 web/admin/server/worker/nginx），本任务**只放骨架**：

```yaml
# docker-compose.prod.yml 骨架
version: '3.9'
services:
  nginx:
    image: nginx:alpine
    ports: ["80:80", "443:443"]
    volumes: [./nginx.conf:/etc/nginx/nginx.conf:ro, ./certs:/etc/nginx/certs:ro]
  web:    # 用户端 H5 静态
    image: ${REGISTRY}/web:${TAG}
  admin:  # 后台 H5 静态
    image: ${REGISTRY}/admin:${TAG}
  server: # NestJS
    image: ${REGISTRY}/server:${TAG}
    env_file: .env
  worker: # Bull Worker
    image: ${REGISTRY}/server:${TAG}
    command: node dist/workers/ai-worker
  postgres: { image: postgres:15-alpine, volumes: [pgdata:/var/lib/postgresql/data] }
  redis: { image: redis:7-alpine }
```

6. **验证**：
   ```bash
   docker compose up -d
   docker compose ps  # 两个服务都 healthy
   docker compose exec postgres psql -U app -d medical_aesthetics -c "SELECT 1"
   docker compose exec redis redis-cli ping  # PONG
   docker compose down
   ```

7. **修改 README.md** 增加开发环境启动说明（docker compose up）

8. 提交：
   ```bash
   git add docker-compose.yml docker-compose.prod.yml .env.example .gitignore scripts/
   git commit -m "chore(docker): 本地 PG + Redis + 环境变量模板"
   ```

**Test**: `docker compose up -d && docker compose ps` 全部 healthy

**Commit**: `chore(docker): 本地 PG + Redis + 环境变量模板`

**Done criteria**:
- `docker compose up -d` 后 `ps` 显示两个服务都 `healthy`
- `psql` 和 `redis-cli ping` 都连得上
- `.env.example` 覆盖所有 `process.env` 用到的变量

**实际 commit**:
- `921245f` chore(docker): 本地 PG + Redis + 环境变量模板（5 文件：docker-compose.yml / docker-compose.prod.yml / .env.example / scripts/init.sql / .gitignore）
- `abba3a0` docs(readme): 升级到 H5 v2.0 架构说明（README 重写）

**⏸ 待陈工本地验证**:
步骤 6 要求的 `docker compose up -d` + `ps healthy` + `psql` + `redis-cli ping` 完整启动验证，需在陈工本地有 Docker Hub 镜像访问的开发机上执行。沙盒环境（Claude Code）已确认：
- `docker compose v2.27.0 config` 解析 `docker-compose.yml` 语法通过（仅 `version` 字段过时警告）
- `docker compose up -d` 命令本身可解析，但镜像拉取因网络限制失败（非配置问题）

---

### ✅ Task 3: NestJS 脚手架 【DONE】

**Goal**: 初始化 NestJS 项目，含 main.ts / app.module.ts / ConfigModule / pino logger / 健康检查 / Swagger

**Pre-reqs**: Task 2 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/package.json`
- Create: `/Users/chenjin/Downloads/beautify_work/server/tsconfig.json`
- Create: `/Users/chenjin/Downloads/beautify_work/server/tsconfig.build.json`
- Create: `/Users/chenjin/Downloads/beautify_work/server/nest-cli.json`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/main.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/app.module.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/app.controller.ts`（健康检查）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/config/configuration.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/config/validation.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/.env`（**加入 .gitignore**）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/main.spec.ts`（冒烟测试）
- Modify: `/Users/chenjin/Downloads/beautify_work/package.json`（加 workspaces）

**Steps**:

1. **写 `server/package.json`**：

```json
{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "node dist/main",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "lint": "eslint \"{src,test}/**/*.ts\" --fix",
    "test": "jest",
    "test:unit": "jest --testPathPattern=src/.*\\.spec\\.ts$",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "test:cov": "jest --coverage"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/config": "^3.1.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-express": "^10.3.0",
    "@nestjs/swagger": "^7.3.0",
    "@nestjs/terminus": "^10.2.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "helmet": "^7.1.0",
    "nestjs-pino": "^4.0.0",
    "pino-http": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.3.0",
    "@nestjs/schematics": "^10.1.0",
    "@nestjs/testing": "^10.3.0",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.5.1",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.0"
  },
  "jest": {
    "moduleFileExtensions": ["js", "json", "ts"],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": { "^.+\\.(t|j)s$": "ts-jest" },
    "collectCoverageFrom": ["**/*.(t|j)s"],
    "coverageDirectory": "../coverage",
    "testEnvironment": "node"
  }
}
```

2. **写 `server/tsconfig.json`**：

```json
{
  "compilerOptions": {
    "module": "commonjs",
    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "target": "ES2022",
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": "./",
    "incremental": true,
    "skipLibCheck": true,
    "strict": true,
    "strictNullChecks": true,
    "noImplicitAny": true,
    "strictBindCallApply": true,
    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "paths": {
      "@/*": ["src/*"],
      "@common/*": ["src/common/*"],
      "@modules/*": ["src/modules/*"],
      "@config/*": ["src/config/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

3. **写 `server/src/main.ts`**：

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { Logger } from 'nestjs-pino';
import helmet from 'helmet';
import { AppModule } from './app.module';
import { HttpExceptionFilter } from './common/filters/http-exception.filter';
import { TransformInterceptor } from './common/interceptors/transform.interceptor';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));

  app.use(helmet());
  app.enableCors({
    origin: [process.env.FRONTEND_URL, process.env.ADMIN_URL].filter(Boolean),
    credentials: true,
  });

  app.setGlobalPrefix('api');
  app.useGlobalPipes(new ValidationPipe({ transform: true, whitelist: true, forbidNonWhitelisted: true }));
  app.useGlobalFilters(new HttpExceptionFilter());
  app.useGlobalInterceptors(new TransformInterceptor());

  if (process.env.NODE_ENV !== 'production') {
    const config = new DocumentBuilder().setTitle('医美咨询 H5 API').setVersion('2.0').build();
    const document = SwaggerModule.createDocument(app, config);
    SwaggerModule.setup('api/docs', app, document);
  }

  const port = parseInt(process.env.PORT || '3000', 10);
  await app.listen(port);
  app.get(Logger).log(`🚀 Server running on http://localhost:${port}/api`, 'Bootstrap');
}
bootstrap();
```

4. **写 `server/src/app.module.ts`**：

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { TerminusModule } from '@nestjs/terminus';
import { LoggerModule } from 'nestjs-pino';
import configuration from './config/configuration';
import { validationSchema } from './config/validation';
import { AppController } from './app.controller';

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      load: [configuration],
      validationSchema,
      validationOptions: { allowUnknown: true, abortEarly: false },
    }),
    LoggerModule.forRootAsync({
      useFactory: () => ({
        pinoHttp: {
          level: process.env.NODE_ENV === 'production' ? 'info' : 'debug',
          transport: process.env.NODE_ENV !== 'production' ? { target: 'pino-pretty' } : undefined,
          redact: ['req.headers.authorization', 'req.headers.cookie'],
        },
      }),
    }),
    TerminusModule,
  ],
  controllers: [AppController],
})
export class AppModule {}
```

5. **写 `server/src/app.controller.ts`**（健康检查）：

```typescript
import { Controller, Get } from '@nestjs/common';
import { HealthCheck, HealthCheckService, HttpHealthIndicator } from '@nestjs/terminus';
import { Public } from './common/decorators/public.decorator';

@Controller()
export class AppController {
  constructor(
    private health: HealthCheckService,
    private http: HttpHealthIndicator,
  ) {}

  @Public()
  @Get('health')
  @HealthCheck()
  check() {
    return this.health.check([async () => this.http.pingCheck('self', 'http://localhost:' + (process.env.PORT || 3000))]);
  }
}
```

6. **写 `server/src/config/configuration.ts`**：

```typescript
export default () => ({
  port: parseInt(process.env.PORT || '3000', 10),
  database: { url: process.env.DATABASE_URL },
  redis: { url: process.env.REDIS_URL },
  jwt: {
    secret: process.env.JWT_SECRET,
    expiresIn: process.env.JWT_EXPIRES_IN || '30m',
    refreshSecret: process.env.JWT_REFRESH_SECRET,
    refreshExpiresIn: process.env.JWT_REFRESH_EXPIRES_IN || '7d',
  },
  encrypt: { key: process.env.FIELD_ENCRYPT_KEY },
  frontendUrl: process.env.FRONTEND_URL,
  adminUrl: process.env.ADMIN_URL,
});
```

7. **写 `server/src/config/validation.ts`**（用 Joi）：

```typescript
import * as Joi from 'joi';

export const validationSchema = Joi.object({
  NODE_ENV: Joi.string().valid('development', 'test', 'production').default('development'),
  PORT: Joi.number().default(3000),
  DATABASE_URL: Joi.string().required(),
  REDIS_URL: Joi.string().required(),
  JWT_SECRET: Joi.string().min(16).required(),
  JWT_REFRESH_SECRET: Joi.string().min(16).required(),
  FIELD_ENCRYPT_KEY: Joi.string().min(32).required(),
  FRONTEND_URL: Joi.string().uri().required(),
  ADMIN_URL: Joi.string().uri().required(),
});
```

8. **写 `server/src/main.spec.ts`**（冒烟测试，确保 main 装配不报）：

```typescript
import { Test } from '@nestjs/testing';
import { AppModule } from './app.module';

describe('AppModule (smoke)', () => {
  it('compiles', async () => {
    const module = await Test.createTestingModule({ imports: [AppModule] }).compile();
    expect(module).toBeDefined();
  });
});
```

9. **写占位**：`server/src/common/filters/http-exception.filter.ts` 和 `server/src/common/interceptors/transform.interceptor.ts` 和 `server/src/common/decorators/public.decorator.ts`（**只放骨架**，Task 5 完整实现）

10. **修改根 `package.json`** 加 server 到 workspaces：
```json
"workspaces": ["web", "admin", "server", "shared"]
```

11. 装依赖 + 跑冒烟测试：
```bash
cd /Users/chenjin/Downloads/beautify_work
npm install
cd server
cp .env.example .env
npm run test:unit -- main.spec.ts
```

12. 提交：
```bash
git add server/ package.json package-lock.json
git commit -m "feat(server): NestJS 脚手架 + 配置 + 日志 + 健康检查 + Swagger"
```

**Test**: `npm run test:unit -- main.spec.ts` PASS

**Commit**: `feat(server): NestJS 脚手架 + 配置 + 日志 + 健康检查 + Swagger`

**Done criteria**:
- `npm run start:dev` 启动后访问 `http://localhost:3000/api/health` 返回 `{ status: 'ok' }`
- 访问 `http://localhost:3000/api/docs` 看到 Swagger UI
- `npm run test:unit` 冒烟测试通过
- `.env` 在 `.gitignore` 中

**实际 commit**: `d000701` — `feat(server): NestJS 脚手架 + 配置 + 日志 + 健康检查 + Swagger`（15 文件，9133 行；含根 `package.json` 升级到 v2.0 workspaces）

**实现差异 vs Runbook 模板**:
- **env 校验库**：用 **class-validator + class-transformer**（`plainToInstance` + `validateSync`）替代 Runbook 模板的 **joi**。陈工已确认：与 NestJS 生态一致，少一个依赖。EnvVars 类用 `!:` 断言字段必填（绕过 strict 模式的 `strictPropertyInitialization`）。
- **common/ 范围**：按 Runbook 步骤 9，**只放 3 个占位**（`@Public` / `HttpExceptionFilter` / `TransformInterceptor`）。guards/filter 完整实现留 Task 5。
- **app.controller.ts**：Terminus `health.check([])` 传空数组（不加内置 `HttpHealthIndicator`，因为它的 `pingCheck` 会自己起 server 易递归；后续 Task 4+ 加 `TypeOrmHealthIndicator` / `RedisHealthIndicator`）。
- **CORS origin**：`[FRONTEND_URL, ADMIN_URL].filter(Boolean)` 用了 `as string[]` 类型断言，因 `filter(Boolean)` 返回类型在 strict 下推不出 string[]。

**验证记录**（沙盒 + 陈工本地均通过）:
- `npm run test:unit -- main.spec.ts` → **PASS** (AppModule compiles, 41ms)
- `npm run start:dev` → Nest 0 errors，`Mapped {/api/health, GET}`，端口 3000 监听
- `curl /api/health` → HTTP 200, `{"code":0,"message":"ok","data":{"status":"ok",...}}`
- `curl /api/docs` → HTTP 200, HTML 含 "Swagger UI"
- `curl /api/docs-json` → HTTP 200, OpenAPI 3.0.0, title=医美咨询 H5 API
- `curl /api/nonexistent` → HTTP 404，被 `HttpExceptionFilter` 包成 `{code:404, message, data, path, timestamp}` 全局格式
- `git check-ignore server/.env server/node_modules server/dist` → 全部 ignore 命中 ✓

---

### ✅ Task 4: TypeORM + Migration 框架 【DONE】

**Goal**: 集成 TypeORM，14 张实体 + Migration runner + DataSource

**Pre-reqs**: Task 3 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/config/typeorm.config.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/entities/{user,credit-package,order,generation,preset-item,credit-ledger,system-config,ai-call-log,download-log,refund,admin-user,user-agreement,sms-code,notification}.entity.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/migrations/1717400000000-InitSchema.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/migrations/1717400000001-SeedPresetsAndConfigs.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/config/typeorm.config.spec.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/app.module.ts`

**Steps**:

1. **装依赖**：
```bash
cd server && npm install @nestjs/typeorm typeorm pg
npm install --save-dev @types/pg
```

2. **写 `server/src/config/typeorm.config.ts`**（DataSource 给 migration CLI 用）：

```typescript
import { DataSource, DataSourceOptions } from 'typeorm';
import * as dotenv from 'dotenv';
import { join } from 'path';
import { User } from '../entities/user.entity';
import { CreditPackage } from '../entities/credit-package.entity';
import { Order } from '../entities/order.entity';
import { Generation } from '../entities/generation.entity';
import { PresetItem } from '../entities/preset-item.entity';
import { CreditLedger } from '../entities/credit-ledger.entity';
import { SystemConfig } from '../entities/system-config.entity';
import { AiCallLog } from '../entities/ai-call-log.entity';
import { DownloadLog } from '../entities/download-log.entity';
import { Refund } from '../entities/refund.entity';
import { AdminUser } from '../entities/admin-user.entity';
import { UserAgreement } from '../entities/user-agreement.entity';
import { SmsCode } from '../entities/sms-code.entity';
import { Notification } from '../entities/notification.entity';
import { InitSchema1717400000000 } from '../migrations/1717400000000-InitSchema';
import { SeedPresetsAndConfigs1717400000001 } from '../migrations/1717400000001-SeedPresetsAndConfigs';

dotenv.config();

export const dataSourceOptions: DataSourceOptions = {
  type: 'postgres',
  url: process.env.DATABASE_URL,
  entities: [User, CreditPackage, Order, Generation, PresetItem, CreditLedger, SystemConfig, AiCallLog, DownloadLog, Refund, AdminUser, UserAgreement, SmsCode, Notification],
  migrations: [InitSchema1717400000000, SeedPresetsAndConfigs1717400000001],
  synchronize: false,
  logging: process.env.NODE_ENV === 'development' ? ['error', 'warn', 'migration'] : ['error'],
};

const dataSource = new DataSource(dataSourceOptions);
export default dataSource;
```

3. **示例：写 `user.entity.ts`**（其余 13 张同模式，按 design.md §4 字段）：

```typescript
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, Index, OneToMany } from 'typeorm';
import { Exclude } from 'class-transformer';
import { CreditLedger } from './credit-ledger.entity';
import { Order } from './order.entity';
import { Generation } from './generation.entity';

export enum UserStatus { ACTIVE = 'active', BANNED = 'banned', DELETED = 'deleted' }

@Entity('users')
@Index('idx_users_phone', ['phoneHash'])
@Index('idx_users_status', ['status'])
@Index('idx_users_created_at', ['createdAt'])
export class User {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @Column({ type: 'varchar', length: 64, unique: true })
  phoneHash: string;

  @Exclude({ toPlainOnly: true })
  @Column({ type: 'text' })
  phoneEncrypted: string;

  @Column({ type: 'varchar', length: 50, nullable: true })
  nickname: string | null;

  @Column({ type: 'text', nullable: true })
  avatar: string | null;

  @Column({ type: 'int', default: 0 })
  credits: number;

  @Column({ type: 'enum', enum: UserStatus, default: UserStatus.ACTIVE })
  status: UserStatus;

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;

  @Column({ type: 'timestamptz', nullable: true, name: 'deleted_at' })
  deletedAt: Date | null;

  @OneToMany(() => CreditLedger, (l) => l.user)
  ledger: CreditLedger[];

  @OneToMany(() => Order, (o) => o.user)
  orders: Order[];

  @OneToMany(() => Generation, (g) => g.user)
  generations: Generation[];
}
```

> 其余 13 张实体照 design.md §4 字段一一对应写出来。篇幅限制此处只列 user；其他用同样模式：
> - `credit-package.entity.ts` (id, name, credits, priceCents, bonusCredits, validityDays, isActive, sortOrder)
> - `order.entity.ts` (id, orderNo, userId, packageId, credits, amountCents, status, paymentMethod, txnId, paidAt, createdAt)
> - `generation.entity.ts` (id, userId, originalUrl, resultUrl, presetKeys, promptText, modelUsed, creditsCost, status, errorMsg, expiresAt, createdAt)
> - `preset-item.entity.ts` (id, key, category, name, description, defaultPrompt, creditsCost, isActive, sortOrder)
> - `credit-ledger.entity.ts` (id, userId, type, amount, balanceAfter, relatedId, note, createdAt)
> - `system-config.entity.ts` (key, value JSONB, updatedAt, updatedBy)
> - `ai-call-log.entity.ts` (id, userId, generationId, model, requestSize, responseSize, costCents, latencyMs, success, errorCode, createdAt)
> - `download-log.entity.ts` (id, userId, generationId, ip, ua, createdAt)
> - `refund.entity.ts` (id, orderId, userId, reason, status, amountCents, operatorId, createdAt, completedAt)
> - `admin-user.entity.ts` (id, username, passwordHash, role, lastLoginAt)
> - `user-agreement.entity.ts` (id, userId, type, version, acceptedAt, UNIQUE(userId, type, version))
> - `sms-code.entity.ts` (phone, code, expiresAt, used, createdAt)
> - `notification.entity.ts` (id, userId, type, title, body, payload, readAt, createdAt)

4. **写 Migration `1717400000000-InitSchema.ts`**（建 14 张表的 DDL）：

```typescript
import { MigrationInterface, QueryRunner } from 'typeorm';

export class InitSchema1717400000000 implements MigrationInterface {
  name = 'InitSchema1717400000000';

  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`CREATE EXTENSION IF NOT EXISTS "uuid-ossp"`);
    await queryRunner.query(`CREATE EXTENSION IF NOT EXISTS "pgcrypto"`);

    await queryRunner.query(`
      CREATE TYPE user_status AS ENUM ('active', 'banned', 'deleted')
    `);
    await queryRunner.query(`
      CREATE TABLE users (
        id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
        phone_hash VARCHAR(64) UNIQUE NOT NULL,
        phone_encrypted TEXT NOT NULL,
        nickname VARCHAR(50),
        avatar TEXT,
        credits INTEGER NOT NULL DEFAULT 0,
        status user_status NOT NULL DEFAULT 'active',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        deleted_at TIMESTAMPTZ
      )
    `);
    await queryRunner.query(`CREATE INDEX idx_users_status ON users(status)`);
    await queryRunner.query(`CREATE INDEX idx_users_created_at ON users(created_at DESC)`);

    // ... 其余 13 张表 DDL 略，按 entities 一一对应
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`DROP TABLE IF EXISTS users CASCADE`);
    await queryRunner.query(`DROP TYPE IF EXISTS user_status`);
    // ... 其余 13 张表 DROP
  }
}
```

5. **写 Migration `1717400000001-SeedPresetsAndConfigs.ts`**：插入 20 个 preset + 4 个 package + 9 条 system_configs（数据从 plan §002_seed_data.js 迁移）

6. **修改 `app.module.ts`** 加 TypeOrmModule.forRootAsync：

```typescript
import { TypeOrmModule } from '@nestjs/typeorm';
import { dataSourceOptions } from './config/typeorm.config';

@Module({
  imports: [
    // ... 已有 imports
    TypeOrmModule.forRootAsync({ useFactory: () => dataSourceOptions }),
  ],
})
export class AppModule {}
```

7. **写 `typeorm.config.spec.ts`**（验证 DataSource 配置正确）：

```typescript
import dataSource from './typeorm.config';

describe('DataSource config', () => {
  it('initializes', async () => {
    await expect(dataSource.initialize()).resolves.toBeDefined();
    await dataSource.destroy();
  }, 30000);
});
```

8. **跑迁移**（需 docker PG 启动）：
```bash
cd server
npm run typeorm migration:run
```

9. 提交：
```bash
git add server/src/entities/ server/src/migrations/ server/src/config/typeorm.config.ts
git commit -m "feat(db): TypeORM + 14 实体 + migration 框架 + 种子数据"
```

**Test**: `npm run test:unit -- typeorm.config.spec.ts` PASS

**Commit**: `feat(db): TypeORM + 14 实体 + migration 框架 + 种子数据`

**Done criteria**:
- `npm run typeorm migration:run` 成功，14 张表 + 20 个 preset + 4 个 package + 9 条 system_configs 在 DB 中
- `psql ... \dt` 看到 14 张表
- `psql ... SELECT COUNT(*) FROM preset_items` = 20
- `psql ... SELECT COUNT(*) FROM credit_packages` = 4
- `psql ... SELECT COUNT(*) FROM system_configs` = 9

**实际 commit**: `0b9b364` — `feat(db): TypeORM + 14 实体 + migration 框架 + 种子数据`（21 文件，9427 行）

**实现差异 vs Runbook 规范**:
- **migration 时间戳**：用 **标准 TypeORM CLI 格式**（`1700000000000` / `1700000000001`）替代 Runbook 模板的 `1717400000000` — 陈工确认。
- **索引范围**：按陈工决定**只建主表结构**（PK + 必要 unique 约束：phoneHash / orderNo / presetKey / adminUsername / userAgreement 的 `(userId, type, version)` 联合唯一）。性能索引（`status` / `created_at DESC` / `(user_id, created_at)` 复合）**留后续优化阶段**。命名风格定为 `uq_<table>_<col>` / `idx_<table>_<col>` snake_case，**覆盖全表**。
- **system_configs.value 标量值**：用 `jsonb_build_object('v', <val>)` 包成对象，应用层读 `cfg.value.v`。原因：`to_jsonb('text')` 在 typeorm bind 时 typeorm 走 pg protocol 丢失 polymorphic context（PG 抛 `42804`）；object 形式 `::jsonb` 也不允许裸 string。统一包对象最干净。
- **preset 数量**：原 Runbook 隐含 20 个（`5×4`），实际表设计 4+5+5+5=19，已补 `rhinoplasty_septum`（鼻中隔矫正）凑 20。
- **migrationsRun**：仅 `NODE_ENV=production` 自动跑，开发环境用 typeorm CLI 显式 `migration:run`（便于回退/重放）。
- **DataSource 路径**：用陈工的 workspaces 结构，`typeorm.config.ts` 在 `server/src/config/`，`npm run typeorm` script 显式 `--dataSource` 指向。

**验证记录**（沙盒环境）:
- `npm run test:unit` → **2/2 PASS**（`main.spec.ts` 41ms + `typeorm.config.spec.ts` 5.2s）
- `npm run typeorm migration:run` → 2/2 成功（InitSchema + Seed）
- `psql \dt` → **15 张表**（14 业务 + `typeorm_migrations`）
- `SELECT COUNT(*) FROM preset_items` = **20** ✓
- `SELECT COUNT(*) FROM credit_packages` = **4** ✓
- `SELECT COUNT(*) FROM system_configs` = **9** ✓
- 验证 packages: 体验(50, 0, ¥29) / 标准(220, 20, ¥99) / 专业(580, 80, ¥229) / 旗舰(2400, 400, ¥899)
- 验证 sysconfig keys: protocols.user, protocols.privacy, credit_pricing_table, rate_limit, ai_models, prompt_prefix, prompt_suffix, generation_expiry_days, copyright_notice

---

### ✅ Task 5: 公共模块 【DONE】

**Goal**: 6 个公共件 + 3 个 decorator

**Pre-reqs**: Task 3 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/decorators/public.decorator.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/decorators/current-user.decorator.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/decorators/roles.decorator.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/guards/jwt-auth.guard.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/guards/admin-auth.guard.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/guards/roles.guard.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/filters/http-exception.filter.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/interceptors/transform.interceptor.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/dto/api-response.dto.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/exceptions/business.exceptions.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/main.ts`

**Steps**:

1. **TDD 写测试**（先写 `auth.guard.spec.ts`、`transform.interceptor.spec.ts` 见 plan.md Task 5）

2. **跑测试看到 FAIL**

3. **实现 decorator**：

`public.decorator.ts`：
```typescript
import { SetMetadata } from '@nestjs/common';
export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

`current-user.decorator.ts`：
```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';
export interface JwtPayload { sub: string; phone: string; type: 'user' | 'admin' }
export const CurrentUser = createParamDecorator((_: unknown, ctx: ExecutionContext): JwtPayload => {
  return ctx.switchToHttp().getRequest().user;
});
```

`roles.decorator.ts`：
```typescript
import { SetMetadata } from '@nestjs/common';
export const ROLES_KEY = 'roles';
export type Role = 'user' | 'admin' | 'super';
export const Roles = (...roles: Role[]) => SetMetadata(ROLES_KEY, roles);
```

4. **实现 guards**（完整 TDD，先写 mock 测试再实现）：

`jwt-auth.guard.ts`（**Task 10 才有完整 JwtStrategy，本任务先放 mock 通过即可**）：
```typescript
import { Injectable, ExecutionContext, UnauthorizedException } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from '../decorators/public.decorator';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private reflector: Reflector) { super(); }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [context.getHandler(), context.getClass()]);
    if (isPublic) return true;
    return super.canActivate(context);
  }

  handleRequest(err: any, user: any) {
    if (err || !user) throw err || new UnauthorizedException('未登录');
    return user;
  }
}
```

`admin-auth.guard.ts`：
```typescript
import { Injectable, ExecutionContext, ForbiddenException } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from '../decorators/public.decorator';
import { ROLES_KEY, Role } from '../decorators/roles.decorator';

@Injectable()
export class AdminAuthGuard extends AuthGuard('jwt') {
  constructor(private reflector: Reflector) { super(); }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [context.getHandler(), context.getClass()]);
    if (isPublic) return true;
    return super.canActivate(context);
  }

  handleRequest(err: any, user: any) {
    if (err || !user) throw err || new ForbiddenException('需要管理员权限');
    if (user.type !== 'admin' && user.role !== 'super' && user.role !== 'admin') {
      throw new ForbiddenException('需要管理员权限');
    }
    return user;
  }
}
```

`roles.guard.ts`：
```typescript
import { Injectable, CanActivate, ExecutionContext, ForbiddenException } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY, Role } from '../decorators/roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const required = this.reflector.getAllAndOverride<Role[]>(ROLES_KEY, [context.getHandler(), context.getClass()]);
    if (!required || required.length === 0) return true;
    const { user } = context.switchToHttp().getRequest();
    if (!user || !required.includes(user.role)) {
      throw new ForbiddenException('权限不足');
    }
    return true;
  }
}
```

5. **实现 filter**：

`http-exception.filter.ts`：
```typescript
import { ExceptionFilter, Catch, ArgumentsHost, HttpException, HttpStatus, Logger } from '@nestjs/common';
import { Request, Response } from 'express';

@Catch()
export class HttpExceptionFilter implements ExceptionFilter {
  private readonly logger = new Logger(HttpExceptionFilter.name);

  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const res = ctx.getResponse<Response>();
    const req = ctx.getRequest<Request>();

    let status = HttpStatus.INTERNAL_SERVER_ERROR;
    let message: string | object = 'Internal Server Error';
    let code = 500;

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      const r = exception.getResponse();
      if (typeof r === 'string') { message = r; }
      else if (typeof r === 'object' && r !== null) {
        message = (r as any).message ?? r;
        code = (r as any).code ?? status;
      }
    } else if (exception instanceof Error) {
      message = exception.message;
      this.logger.error(exception.stack);
    }

    res.status(status).json({ code, message, data: null, path: req.url, timestamp: new Date().toISOString() });
  }
}
```

6. **实现 interceptor**：

`transform.interceptor.ts`：
```typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable, map } from 'rxjs';

export interface ApiResponse<T> { code: number; message: string; data: T; }

@Injectable()
export class TransformInterceptor<T> implements NestInterceptor<T, ApiResponse<T>> {
  intercept(_: ExecutionContext, next: CallHandler): Observable<ApiResponse<T>> {
    return next.handle().pipe(map((data) => ({ code: 0, message: 'ok', data })));
  }
}
```

7. **实现 exceptions**：

`business.exceptions.ts`：
```typescript
import { HttpException, HttpStatus } from '@nestjs/common';
import { CODES } from './codes';

export class BusinessException extends HttpException {
  constructor(code: number, message: string, status: number = HttpStatus.OK) {
    super({ code, message }, status);
  }
}
export const InsufficientCreditsException = () => new BusinessException(CODES.INSUFFICIENT_CREDITS, '积分不足', HttpStatus.PAYMENT_REQUIRED);
export const RateLimitedException = () => new BusinessException(CODES.RATE_LIMITED, '操作过于频繁，请稍后再试', HttpStatus.TOO_MANY_REQUESTS);
export const NotFoundException = (msg = '资源不存在') => new BusinessException(CODES.NOT_FOUND, msg);
```

8. **修改 `main.ts`** 在 main.ts 引入（已在 Task 3 引入，本任务确保不漏）

9. **跑测试看到 PASS**

10. 提交：
```bash
git add server/src/common/
git commit -m "feat(server): 公共模块（guards/filters/interceptors/decorators）"
```

**Test**: `npm run test:unit` PASS（common 下的所有 spec）

**Commit**: `feat(server): 公共模块（guards/filters/interceptors/decorators）`

**Done criteria**:
- 6 个公共件 + 3 个 decorator 存在
- 单元测试通过
- 集成到 main.ts 后 `npm run start:dev` 仍能起来

**实际 commit**: `7fdd112` — `feat(server): 公共模块（guards/filters/interceptors/decorators/exceptions）`（17 文件，799 行）

**实现差异 vs Runbook 规范**:
- **CODES 单文件**: 按陈工决定 `common/codes.ts` 数字 enum 集中管理（21 个码按 1xxx/2xxx/3xxx/4xxx/5xxx 分组）。`BusinessException` 与工厂函数（`InsufficientCreditsException()` 等）从 codes.ts 取值。
- **JwtStrategy 走 mock**: 按陈工决定，本任务 guard 单元测试通过 `@Public()` 分支 + `handleRequest` 直接覆盖，**不注册真实 JwtStrategy**（留 Task 10）。guard 实际部署时，passport 会报 `Unknown auth strategy 'jwt'`，故启动 dev server 验证只跑单元测试，不启服务。
- **全局注册方式**: 用 **`APP_GUARD` provider** 注入 `JwtAuthGuard` + `RolesGuard`（而非 `app.useGlobalGuards(new ...)` 直接 new）— 保证 guard 通过 Nest DI 拿到 `Reflector`，未来 Task 10 接入真实 JwtStrategy 时无需改 main.ts。
- **HttpExceptionFilter 修复**: 之前 Task 3 占位版对 HttpException string-body 分支忘了同步 `code = status`，导致抛 `new HttpException('bad', 400)` 时 `code` 是默认 500。已修。
- **Role 推导**: `RolesGuard` 把 `user.type === 'admin'` 推导为 `role = 'admin'`（向后兼容 JwtPayload 旧 shape），同时 `user.role` 优先。
- **AdminAuthGuard 通过条件**: `type === 'admin'` OR `role in {'admin','super'}` 三选一放行。

**验证记录**（沙盒环境）:
- `npm run test:unit` → **24/24 PASS**（7 suite：main + typeorm.config + filter + interceptor + 3 guard）
- `npm run test:unit -- main.spec.ts` → AppModule 仍 compile 通过（含 APP_GUARD provider 注册）
- `npm run start:dev` 未在沙盒启动（passport 找不到 'jwt' strategy，预期行为；Task 10 后才会正常启服务）

---

### ✅ Task 6: Web 端 H5 脚手架 【DONE】

**Goal**: 初始化 web/ 目录（Vue 3 + Vite + Vant + Pinia + Router + Axios 封装 + 响应式 mixin）

**Pre-reqs**: Task 1 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/web/package.json`
- Create: `/Users/chenjin/Downloads/beautify_work/web/vite.config.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/index.html`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/main.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/App.vue`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/router.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/stores/user.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/api/index.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/api/auth.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/utils/responsive.js`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/styles/variables.scss`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/styles/global.scss`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/views/Layout.vue`
- Create: `/Users/chenjin/Downloads/beautify_work/web/src/views/Dashboard.vue`（占位）
- Create: `/Users/chenjin/Downloads/beautify_work/web/tests/unit/responsive.spec.js`
- Modify: `/Users/chenjin/Downloads/beautify_work/package.json`（workspaces）

**Steps**:

1. **写 `web/package.json`**：

```json
{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "pinia": "^2.1.7",
    "vant": "^4.8.3",
    "vue": "^3.4.0",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/test-utils": "^2.4.0",
    "jsdom": "^23.0.0",
    "sass": "^1.69.0",
    "unplugin-vue-components": "^0.26.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
```

2. **写 `web/vite.config.js`**：

```javascript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import Components from 'unplugin-vue-components/vite';
import { VantResolver } from '@vant/auto-import-resolver';
import path from 'path';

export default defineConfig({
  plugins: [
    vue(),
    Components({ resolvers: [VantResolver()] }),
  ],
  resolve: {
    alias: { '@': path.resolve(__dirname, 'src') },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': { target: 'http://localhost:3000', changeOrigin: true },
    },
  },
  test: { environment: 'jsdom', globals: true },
});
```

3. **写 `web/index.html`**：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
  <meta name="theme-color" content="#d4a5a0" />
  <title>医美咨询 AI 预览</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

4. **写 `web/src/main.js`**：

```javascript
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import App from './App.vue';
import router from './router';
import 'vant/lib/index.css';
import './styles/global.scss';

const app = createApp(App);
app.use(createPinia());
app.use(router);
app.mount('#app');
```

5. **写 `web/src/App.vue`**：

```vue
<template>
  <router-view />
</template>
<script setup>
import { onMounted } from 'vue';
import { useUserStore } from '@/stores/user';
const user = useUserStore();
onMounted(() => user.fetchProfile());
</script>
```

6. **写 `web/src/router.js`**（路由 + 守卫）：

```javascript
import { createRouter, createWebHistory } from 'vue-router';
import { useUserStore } from '@/stores/user';

const routes = [
  { path: '/login', name: 'Login', component: () => import('@/views/Login.vue'), meta: { public: true } },
  { path: '/', component: () => import('@/views/Layout.vue'), children: [
    { path: '', name: 'Dashboard', component: () => import('@/views/Dashboard.vue') },
    // 后续 Task 28-30 补
  ]},
  { path: '/:pathMatch(.*)*', name: 'NotFound', component: () => import('@/views/NotFound.vue'), meta: { public: true } },
];

const router = createRouter({ history: createWebHistory(), routes });

router.beforeEach((to) => {
  if (to.meta.public) return true;
  const user = useUserStore();
  if (!user.token) return { name: 'Login', query: { redirect: to.fullPath } };
  return true;
});

export default router;
```

7. **写 `web/src/api/index.js`**（Axios 封装）：

```javascript
import axios from 'axios';
import { showToast } from 'vant';
import { useUserStore } from '@/stores/user';
import router from '@/router';

const api = axios.create({ baseURL: '/api', timeout: 30000 });

api.interceptors.request.use((config) => {
  const user = useUserStore();
  if (user.token) config.headers.Authorization = `Bearer ${user.token}`;
  return config;
});

api.interceptors.response.use(
  (res) => {
    const { code, message, data } = res.data;
    if (code === 0) return data;
    if (code === 401) {
      useUserStore().logout();
      router.push({ name: 'Login' });
      showToast('请重新登录');
      return Promise.reject(new Error(message));
    }
    showToast(message || '请求失败');
    return Promise.reject(new Error(message));
  },
  (err) => {
    showToast(err.message || '网络错误');
    return Promise.reject(err);
  }
);

export default api;
```

8. **写 `web/src/api/auth.js`**：

```javascript
import api from './index';
export const sendSms = (phone) => api.post('/auth/sms/send', { phone });
export const login = (phone, code) => api.post('/auth/login', { phone, code });
export const refresh = () => api.post('/auth/refresh');
```

9. **写 `web/src/stores/user.js`**（Pinia）：

```javascript
import { defineStore } from 'pinia';
import * as authApi from '@/api/auth';
import api from '@/api';

export const useUserStore = defineStore('user', {
  state: () => ({ token: localStorage.getItem('token') || null, refreshToken: localStorage.getItem('refreshToken') || null, profile: null }),
  actions: {
    async login(phone, code) {
      const data = await authApi.login(phone, code);
      this.token = data.token;
      this.refreshToken = data.refreshToken;
      this.profile = data.user;
      localStorage.setItem('token', data.token);
      localStorage.setItem('refreshToken', data.refreshToken);
    },
    async fetchProfile() { if (this.token) this.profile = await api.get('/user/me'); },
    logout() { this.token = null; this.refreshToken = null; this.profile = null; localStorage.clear(); },
  },
});
```

10. **写 `web/src/utils/responsive.js`**（响应式断点）：

```javascript
export const BREAKPOINTS = { mobile: 768, tablet: 1024 };
export const isMobile = () => window.innerWidth < BREAKPOINTS.mobile;
export const isTablet = () => window.innerWidth >= BREAKPOINTS.mobile && window.innerWidth < BREAKPOINTS.tablet;
export const isDesktop = () => window.innerWidth >= BREAKPOINTS.tablet;
```

11. **写 `web/src/styles/variables.scss`**（Vant 主题覆盖）：

```scss
:root {
  --van-primary-color: #d4a5a0;
  --van-background-2: #f5ebe7;
  --van-text-color: #3d2e2a;
  --van-border-radius: 10px;
}
```

12. **写 `web/src/styles/global.scss`**：

```scss
@import './variables.scss';
html, body, #app { height: 100%; margin: 0; }
body { font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', sans-serif; background: var(--van-background-2); }
```

13. **写占位 `Dashboard.vue` / `Layout.vue` / `Login.vue` / `NotFound.vue`**（每页只放一个标题 + 简单骨架，Task 28-30 完整实现）

14. **写测试 `tests/unit/responsive.spec.js`**（TDD，验证 BREAKPOINTS 正确）

15. **修改根 `package.json`** workspaces 加 `web`

16. **跑测试**：
```bash
cd /Users/chenjin/Downloads/beautify_work
npm install
cd web && npm run test:unit
```

17. 提交：
```bash
git add web/ package.json package-lock.json
git commit -m "feat(web): Vue 3 + Vite + Vant 脚手架 + Pinia + Router + Axios"
```

**Test**: `npm run test:unit` PASS + `npm run dev` 起 http://localhost:5173 看到空白布局（占位 Dashboard）

**Commit**: `feat(web): Vue 3 + Vite + Vant 脚手架 + Pinia + Router + Axios`

**Done criteria**:
- `npm run dev` 起 http://localhost:5173 看到 Vant 默认主题
- 手机端（窄屏）显示移动布局，平板/PC 显示桌面布局（响应式）
- 路由 /login 跳转不报
- 单元测试通过

**实际 commit**: `aa3532d` — `feat(web): Vue 3 + Vite + Vant 脚手架 + Pinia 持久化 + Router + Axios`（19 文件，2972 行）

**实现差异 vs Runbook 规范**:
- **Vant 自动按需**（陈工确认）：用 `unplugin-vue-components + @vant/auto-import-resolver`，view 里直接 `<van-button>` / `<van-tabbar>` 即可，**无需手动 import**。unplugin 自动生成 `web/components.d.ts`（已 commit，含 Vant 全部组件类型）。
- **错误提示用 `vant dialog`**（陈工确认）：拦截器分级 — 401 族业务码（3001/3002/3003）→ `showDialog` 标题"登录已过期"+ 跳 login；其他业务码非 0 → `showDialog` 标题"操作失败"；HTTP 5xx → `showDialog` 标题"服务异常"；其他 4xx（参数校验）→ `showToast`（轻量）。
- **Pinia 持久化插件** `pinia-plugin-persistedstate`（陈工确认）：user store 加 `persist: { key: 'medical_aesthetics_user', storage: localStorage }`，替代 Runbook 模板的 `localStorage.setItem('token', ...)` 手动管理。Token 刷新/过期清理都走 store action。
- **响应式扩展**：除 Runbook 模板的 `isMobile/isTablet/isDesktop`，加了 `useBreakpoint()` composable（响应式 ref，监听 resize 返回 'mobile'/'tablet'/'desktop'）。Task 28+ 用得到。
- **Layout 简化**：占位 Layout.vue 包含 van-tabbar 4 项（工作台/生成/历史/我的），后续 Task 28-30 路由补齐。Topbar 显示当前积分（来自 user.credits getter）。
- **CSS imports 顺序**：`main.js` 引入 `global.scss`（已 `@import './variables.scss'`），确保 Vant 主题变量先于组件库加载。
- **TypeScript-like 安全**：`web/vite.config.js` 用 `node:path` + `fileURLToPath` 解析 `__dirname`（ESM 模式无内置 `__dirname`）。

**验证记录**（沙盒环境）:
- `npm run test:unit` → **8/8 PASS**（responsive.spec.js: BREAKPOINTS/isMobile/isTablet/isDesktop 边界测试）
- `npm run dev` → Vite v5.4.21 ready in **642ms**，端口 5173 监听
- `curl /` → HTTP 200，返回 index.html
- `curl /login` → HTTP 200（SPA history 模式，所有未匹配路由返回 index.html）
- `curl /src/main.js` → HTTP 200，Vite 转译正确（pinia + persistedstate + router 全部解析）
- `curl /src/styles/variables.scss` → HTTP 200，SCSS 编译为 `:root { --van-primary-color: #d4a5a0; ... }` 注入
- `curl /src/views/Login.vue` → HTTP 200，Vue 3 SFC 编译 + HMR runtime 就绪

---

### ✅ Task 7: Admin H5 脚手架 【DONE】

**Goal**: 初始化 admin/ 目录（Vue 3 + Vite + Element Plus + Pinia + Router）

**Pre-reqs**: Task 1 ✅

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/admin/package.json`（参考 web，把 vant 换 element-plus，端口 5174）
- Create: `/Users/chenjin/Downloads/beautify_work/admin/vite.config.js`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/main.js`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/router.js`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/stores/admin.js`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/api/index.js`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/views/Login.vue`（占位）
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/views/Layout.vue`（带侧边栏）
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/views/Dashboard.vue`（占位）
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/styles/variables.scss`
- Modify: `/Users/chenjin/Downloads/beautify_work/package.json`（workspaces）

**Steps**: 与 Task 6 几乎相同，主要差异：
- `package.json` 依赖：`element-plus`（替换 vant），port 5174
- `vite.config.js` ElementPlusResolver（替换 VantResolver）
- `views/Layout.vue` 用 `<el-container>` + `<el-aside>` + `<el-menu>`（PC 后台布局）
- 不需要 `utils/responsive.js`（PC 专用）
- `api/index.js` 拦截器把 token 存到 `admin_token` key
- `stores/admin.js` 与 `stores/user.js` 类似，但 role = 'admin'/'super'

提交：
```bash
git add admin/ package.json package-lock.json
git commit -m "feat(admin): Vue 3 + Vite + Element Plus 脚手架 + Pinia + Router"
```

**Test**: `npm run dev` 起 http://localhost:5174 看到 Element Plus 侧边栏布局（占位）

**Commit**: `feat(admin): Vue 3 + Vite + Element Plus 脚手架 + Pinia + Router`

**Done criteria**:
- 端口 5174 起动
- 看到侧边栏 + 顶栏 + 主区域的 PC 布局
- Element Plus 主题加载（裸粉主色）

**实际 commit**: `3faa762` — `feat(admin): Vue 3 + Vite + Element Plus 脚手架 + Pinia 持久化 + Router`（31 文件，+872/-143 行；清理 9 个 v1 占位 .vue + 3 个 .keep）

**实现差异 vs Runbook 规范**:
- **Element Plus 自动按需**（陈工确认）：用 `unplugin-auto-import + ElementPlusResolver`（API 自动注入如 `ElMessage`/`ElMessageBox`）+ `unplugin-vue-components + ElementPlusResolver`（组件自动注册如 `<el-button>`/`<el-form>`）。**双 resolver**：view 里直接用 `ElMessage.error()` 即可，**无需 import**。unplugin 自动生成 `auto-imports.d.ts` + `components.d.ts`。
- **Login 占位不接 API**（陈工确认）：Login.vue 放完整 `<el-form>` 账号+密码字段 + `<el-button type="primary" @click="onSubmit">`，但 onSubmit 只 `ElMessage.info('请等待 Task 32 接入真实登录 API')`，**不调 store.login / api.loginByPassword**。表单数据存在本地 reactive。
- **Pinia 持久化插件** `pinia-plugin-persistedstate`（陈工确认）：与 web 一致模式，admin store 加 `persist: { key: 'medical_aesthetics_admin', storage: localStorage }`。
- **logout 修复**：原 store logout 用 try/finally，server 抛错时会向上传。改为 try/catch + 注释说明（local state 必须清，避免旧 token 阻塞下次登录）。
- **清理 v1 占位**：`admin/src/pages/{Dashboard,Login,Users,Orders,Packages,Presets,Configs,AILogs,AuditLog}.vue` 9 个 v1 旧 .vue + 3 个 `.keep` 全部删除，Runbook v2.0 改用 `views/` 命名（与 web 端一致）。
- **Layout 折叠**：el-aside 支持折叠（64px ↔ 220px），顶栏 el-button 切换 `Fold/Expand` 图标。
- **路由 redirect**：`/` 重定向到 `/dashboard`（与 web 端直接渲染 Dashboard 不同，后台更适合 entry 重定向）。
- **dev server 端口冲突**：root npm workspaces 模式下 web 启时抢了 5174 端口（5173 + 5174 同指向 web 进程），需 kill 旧 web vite 后再启 admin。admin 配置 `port: 5174` 不变，Vite fallback 5175 表明环境问题。

**验证记录**（沙盒环境）:
- `npm run test:unit` → **4/4 PASS**（admin.store.spec.js: 初始 state / login / isSuper 推导 / logout 即使 server fail 也清空）
- `npm run dev` → Vite v5.4.21 ready in **604ms**，端口 5174 监听
- `curl /` → HTTP 200，返回 index.html
- `curl /login` → HTTP 200（SPA history 模式 fallback index.html）
- `curl /src/views/Login.vue` → HTTP 200，Vue 3 SFC 编译为 `_sfc_main` + `render`
- `curl /src/styles/variables.scss` → HTTP 200，SCSS 编译为 `:root { --el-color-primary: #d4a5a0; ... }` 注入

---

### ✅ Task 8: P0 验收 【DONE】

**Goal**: 跑 P0 所有测试 + 起 docker compose + 验证 web/admin/server 三个端都跑得起来

**Pre-reqs**: Task 1-7 ✅

**Steps**:

1. `cd /Users/chenjin/Downloads/beautify_work && docker compose up -d`
2. `cd server && npm run typeorm migration:run && npm run start:dev &` → http://localhost:3000/api/health OK
3. `cd web && npm run dev &` → http://localhost:5173 看到占位 Dashboard
4. `cd admin && npm run dev &` → http://localhost:5174 看到 Element Plus 布局
5. `cd server && npm run test:unit` 全 PASS
6. **打 tag**：
   ```bash
   git tag p0-foundation
   ```
7. 在 Runbook 任务卡 `⏳` 改 `✅` 填 commit + tag
8. 提交（无新代码，Runbook 文档改动可一并）：
   ```bash
   git add docs/superpowers/execution/2026-06-04-医美咨询H5-runbook.md
   git commit -m "docs(runbook): 标记 P0 (Task 2-8) 为 done"
   ```

**Test**: 4 步全部成功

**Commit**: `docs(runbook): 标记 P0 (Task 2-8) 为 done`

**Done criteria**:
- 所有 Task 2-7 已 commit
- 单元测试全绿
- 4 个端都跑得起来

**实际 tag**: `p0-foundation`（annotated tag，含 P0 任务列表与下一里程碑说明）

**验证记录**（沙盒环境 2026-06-05）:
- `docker compose ps` → **ma-postgres + ma-redis 都 healthy**（4 小时前启动，连续运行）
- `cd server && npm run test:unit` → **7 suite / 24 tests PASS**（main + typeorm.config + filter + interceptor + 3 guard）
- `cd web && npm run test:unit` → **8/8 PASS**（responsive.spec.js）
- `cd admin && npm run test:unit` → **4/4 PASS**（admin.store.spec.js）
- **总计 36/36 tests across 3 workspaces 全绿**
- `curl http://localhost:5173/` → HTTP 200 (web Vant 主题 CSS 注入)
- `curl http://localhost:5173/login` → HTTP 200 (SPA history fallback)
- `curl http://localhost:5174/` → HTTP 200 (admin Element Plus 主题注入)
- `curl http://localhost:5174/login` → HTTP 200 (admin SPA history fallback)
- 三个 dev server 同时跑：server (3000) + web (5173) + admin (5174) ✓

**server 启动状态备注**:
- Task 3 时起的 server dev server (PID 26634, 端口 3000) 是 Task 3 旧版代码
- Task 5 加了 APP_GUARD (`JwtAuthGuard` extends `AuthGuard('jwt')`)，**未接入真实 JwtStrategy 之前 server 启不起来**（passport 抛 `Unknown auth strategy 'jwt'`）
- 这是预期行为：P0 阶段 server 仅完成"基础设施就绪"（env 校验 / TypeORM 接通 / 公共模块 / 单元测试全绿），"server 端真正能跑业务请求"要等 P1 **Task 10** 接入 `JwtStrategy` 后才能启 dev server
- Runbook 步骤 2 描述的 `npm run start:dev` 跑通要到 **P1 验收 (Task 13)** 一起做
- `p0-foundation` tag 已打

---

---

## P1: 用户+协议 【pending】

### ✅ Task 9: SMS 模块 【DONE】

**Goal**: 阿里云短信 SDK 封装，发送 6 位验证码到 Redis，TTL 60s，IP 限流 1/min。

**Pre-reqs**: Task 4（已建表）、Task 5（Redis 注入）、Task 2（Redis 跑通）

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/sms/sms.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/sms/sms.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/sms/sms.module.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/sms/sms.service.spec.ts`（mock SDK）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/sms/rate-limiter.ts`（Lua 限流）

**Steps**:

1. **写失败测试**（`sms.service.spec.ts`）：mock aliyun SDK，断言：
   - `sendCode('13800138000')` 写 Redis key `sms:13800138000` = 6 位数字，TTL 60s
   - 同一手机号 60s 内第二次调 → 抛 429
   - 同一 IP 1min 内第 2 次调 → 抛 429

2. **写 `sms.service.ts`**（关键签名）：

```typescript
// server/src/sms/sms.service.ts
@Injectable()
export class SmsService {
  constructor(
    private readonly redis: RedisService,
    private readonly config: ConfigService,
    private readonly logger: PinoLogger,
  ) {}

  async sendCode(phone: string, ip: string): Promise<{ ok: true; ttl: number }> {
    await this.checkIpRateLimit(ip);
    await this.checkPhoneCooldown(phone);
    const code = randomInt(100000, 999999).toString();
    await this.redis.set(`sms:${phone}`, code, 'EX', 60);
    // (deferred) 真实环境调 aliyun SDK
    if (this.config.get('NODE_ENV') === 'production') {
      await this.aliyunSend(phone, code);
    } else {
      this.logger.warn({ phone, code }, '[dev] sms code');
    }
    return { ok: true, ttl: 60 };
  }

  async verifyCode(phone: string, code: string): Promise<boolean> {
    const stored = await this.redis.get(`sms:${phone}`);
    if (stored === code) {
      await this.redis.del(`sms:${phone}`);
      return true;
    }
    return false;
  }
}
```

3. **写 `rate-limiter.ts`**（Redis Lua 令牌桶 / 简单计数）：

```typescript
// server/src/sms/rate-limiter.ts
export async function checkIpRateLimit(redis: Redis, ip: string, maxPerMin: number) {
  const key = `sms:ip:${ip}`;
  const count = await redis.incr(key);
  if (count === 1) await redis.expire(key, 60);
  if (count > maxPerMin) throw new HttpException('请求过于频繁', 429);
}
```

4. **写 `sms.controller.ts`**（POST `/api/auth/sms/send`，不需鉴权）：

```typescript
@Controller('auth/sms')
export class SmsController {
  constructor(private readonly sms: SmsService) {}
  @Post('send')
  @Public()
  async send(@Body() dto: SendSmsDto, @Req() req: Request) {
    const ip = req.ip;
    const ttl = await this.sms.sendCode(dto.phone, ip);
    return { ok: true, ttl: ttl.ttl };
  }
}
```

5. **Commit**：`feat(sms): aliyun sdk wrapper with redis cooldown + ip rate limit`

**Test**: `curl -X POST /api/auth/sms/send -d '{"phone":"13800138000"}'`

**Commit**: `feat(sms): aliyun sdk wrapper with redis cooldown + ip rate limit`

**Done criteria**:
- dev 环境下 `curl -X POST /api/auth/sms/send -d '{"phone":"13800138000"}'` 200
- 控制台打出 `[dev] sms code 123456`
- 60s 内第二次 429

**实际 commit**: `9c4604c` — `feat(sms): 阿里云短信 SDK wrapper + Redis 冷却 + IP 限流`（8 文件，+773/-3）

**实现差异 vs Runbook 规范**:
- **RedisModule 新建**（Runbook 隐含但未明示）：`server/src/redis/{redis.module.ts, redis.constants.ts}` 提供 `@Global` `REDIS_CLIENT` provider（ioredis 实例，从 `REDIS_URL` 读）。SMS module + 后续 Task 14+ 全部复用此 client。
- **手机号 cooldown key 命名**: 用 `sms:cooldown:${phone}` 区分**验证码 key**（`sms:${phone}`）。Runbook 模板没明示，**推断**：验证码存 `sms:${phone}`，cooldown 计数用 `sms:cooldown:${phone}`。
- **aliyun SDK dev 不导入**: `@alicloud/dysmsapi20170525` 装但**不在 sms.service.ts import**。dev 模式只 `logger.warn({phone, code}, '[dev] sms code')`；production 分支 stub `logger.warn('[sms] production send not yet implemented')`，待 Task 9.x 真实联调时再 lazy import 避免 dev bundle 拖 SDK 体积。
- **TDD spec 顺序**: 先写 `rate-limiter.spec.ts`（独立函数易测）+ `sms.service.spec.ts`（mock REDIS_CLIENT/ConfigService/'PinoLogger:SmsService'），再写实现。
- **PinoLogger 测试 token 修复**: `@InjectPinoLogger(SmsService.name)` 编译成 token `"PinoLogger:SmsService"`（带 context name 后缀），不是 `PinoLogger` class。Spec 第一次用 `PinoLogger` token 注入失败，改 `'PinoLogger:SmsService'` 字符串 token 后通过。
- **ValidationPipe 手机号格式**: controller 用 class-validator `@Matches(/^1[3-9]\d{9}$/)`，无效号码走全局 ValidationPipe → 400 + `手机号格式不正确`，**不到达 SmsService**（IP 限流 + cooldown 不消耗）。
- **rate-limiter 抛 429 走业务码 2020**: 与 `BusinessException` 体系对齐（`RATE_LIMITED = 2020`），HttpExceptionFilter 包装成 `{code:2020, message, data, path, timestamp}` 全局格式。
- **server 启动状态升级**: Task 8 时 server 启不起来（passport 找不到 'jwt'）。Task 9 实际**已能启动**——`/api/health` 和 `/api/auth/sms/send` 都标 `@Public()`，JwtAuthGuard 短路返回 true 不调 super.canActivate，passport 'jwt' 不需要。Task 10 接入真实 JwtStrategy 后所有路由都能跑通。

**验证记录**（沙盒环境 2026-06-05）:
- `npm run test:unit` → **35/35 PASS**（9 suite：原有 7 + 新增 sms.service 7 + rate-limiter 4 = 11 新增）
- `npm run start:dev` → Nest 启动 0 errors，端口 3000
  - 日志确认 `RedisModule dependencies initialized` + `SmsModule dependencies initialized` + `TypeOrmCoreModule dependencies initialized`
  - `Mapped {/api/health, GET}` + `Mapped {/api/auth/sms/send, POST}` route
- `curl /api/health` → HTTP 200 `{status:"ok"}` ✓
- `curl -X POST /api/auth/sms/send -d '{"phone":"13800138000"}'` → **HTTP 200** `{ok:true, ttl:60}` + 日志 `WARN [dev] sms code` ✓
- 60s 内第二次同手机号 → **HTTP 429** `{code:2020, message:"请求过于频繁..."}` ✓
- `curl -X POST /api/auth/sms/send -d '{"phone":"12345"}'` → **HTTP 400** `{code:400, message:["手机号格式不正确"]}`（ValidationPipe 拦截，未达 service）✓
- `redis-cli KEYS 'sms:*'` → 3 个 key:
  - `sms:ip:127.0.0.1` (IP 限流计数, TTL 60s)
  - `sms:13800138000` (6 位验证码, TTL 60s)
  - `sms:cooldown:13800138000` (手机号冷却, TTL 60s)

---

### ✅ Task 10: 登录 + JWT 【DONE】

**Goal**: 手机号 + 验证码 → 签发 access (30min) + refresh (7d)，首次登录写 `users` + 默认昵称。

**Pre-reqs**: Task 9

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/auth/auth.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/auth/auth.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/auth/jwt.strategy.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/auth/dto/login.dto.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/auth/auth.service.spec.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/app.module.ts`（注册 AuthModule、PassportModule、JwtModule）

**Steps**:

1. **写失败测试**：
```typescript
describe('AuthService.login', () => {
  it('creates user on first login', async () => {
    const result = await svc.login({ phone: '13800138000', code: '123456' });
    expect(result.token).toMatch(/^ey/);
    expect(result.user.phone_mask).toBe('138****8000');
  });
});
```

2. **写 `auth.service.ts`**（核心逻辑）：

```typescript
@Injectable()
export class AuthService {
  constructor(
    @InjectRepository(UserEntity) private users: Repository<UserEntity>,
    private sms: SmsService,
    private jwt: JwtService,
    private crypto: CryptoService,
  ) {}

  async login(dto: LoginDto) {
    const ok = await this.sms.verifyCode(dto.phone, dto.code);
    if (!ok) throw new UnauthorizedException('验证码错误或已过期');
    const phoneHash = hashSha256(dto.phone);
    let user = await this.users.findOne({ where: { phone_hash: phoneHash } });
    if (!user) {
      user = this.users.create({
        phone_hash: phoneHash,
        phone_enc: this.crypto.encrypt(dto.phone),
        nickname: `用户${dto.phone.slice(-4)}`,
        credits: 0,
        status: 'active',
      });
      await this.users.save(user);
    }
    if (user.status !== 'active') throw new ForbiddenException('账号已封禁或注销中');
    return {
      token: await this.signAccess(user),
      refresh_token: await this.signRefresh(user),
      user: this.toDto(user),
    };
  }

  async refresh(refreshToken: string) {
    const payload = await this.jwt.verifyAsync(refreshToken);
    const user = await this.users.findOneByOrFail({ id: payload.sub });
    return {
      token: await this.signAccess(user),
      refresh_token: await this.signRefresh(user),
    };
  }

  private async signAccess(user: UserEntity) {
    return this.jwt.signAsync(
      { sub: user.id, type: 'user' },
      { expiresIn: this.config.get('JWT_EXPIRES_IN', '30m') },
    );
  }
  // ... signRefresh / toDto
}
```

3. **写 `jwt.strategy.ts`**：

```typescript
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      secretOrKey: config.get('JWT_SECRET'),
      ignoreExpiration: false,
    });
  }
  async validate(payload: { sub: string; type: string }) {
    return { id: payload.sub, type: payload.type };  // 挂到 req.user
  }
}
```

4. **写 `auth.controller.ts`**：

```typescript
@Controller('auth')
export class AuthController {
  @Post('login') @Public()
  login(@Body() dto: LoginDto) { return this.svc.login(dto); }

  @Post('refresh') @Public()
  refresh(@Body('refresh_token') t: string) { return this.svc.refresh(t); }
}
```

5. **Commit**：`feat(auth): phone+code login with access+refresh jwt`

**Test**: （见 plan.md Task 10 Done Criteria）

**Commit**: `feat(auth): phone+code login with access+refresh jwt`

**Done criteria**:
- 用 dev 控制台打出的验证码，调用 `/api/auth/login` 拿到 token
- 用 token 调 `/api/user/me`（Task 11）通过

**实际 commit**: `bc09391` — `feat(auth): 手机+验证码 登录 + JWT (access 30m / refresh 7d)`（12 文件，+606）

**实现差异 vs Runbook 规范**:
- **AES-256-GCM 加密**（陈工确认）：用 Node 内置 `crypto`，**blob 格式 = `base64(iv).base64(enc).base64(authTag)`**，12 字节 IV 随机 + 16 字节 GCM authTag。智能 key 解析：32 字节 raw / base64 / SHA-256 fallback（确保 dev 任意 `FIELD_ENCRYPT_KEY` 都能工作）。GCM 篡改 authTag 抛 `final()` 错。
- **HS256 JWT**（陈工确认）：passport-jwt 真实实现（`passport-jwt` + `@nestjs/jwt`），secret 从 `JWT_SECRET` env 读。`JwtStrategy.validate(payload)` 返 `{id: payload.sub, type, role?}`，挂到 `req.user`。
- **CryptoModule 新建**（Runbook 隐含）：`@Global` 模块，所有 module 直接 inject CryptoService 无需 import。AuthService + 后续 UserService 共用。
- **DONE criteria #2 待 Task 11**: 用 token 调 `/api/user/me` — Task 10 仅完成 login/refresh，user CRUD 留 Task 11。
- **Mock guard 升级**: Task 5 的 `JwtAuthGuard extends AuthGuard('jwt')` 之前因为没 strategy 启不起来（passport 抛 'jwt' unknown）。Task 10 注册了 `JwtStrategy`，**所有非 @Public 路由现在能跑通**（但 server 启动顺序：passport 在 JwtStrategy 注入后才知道 'jwt' 策略存在）。
- **AuthResult.user DTO**: 不返 `phoneEncrypted`（敏感），返 `phone_mask`（`137****7000`）。credits / nickname / id 都返。
- **refresh token 在 body 而非 Authorization header**: 简化前端（refresh 时 access 通常已过期，不能放 header）。
- **verifyCode 一次性消费**: `redis.del(sms:${phone})` 在 match 后立刻删，防重放。
- **TDD spec 修复**:
  - `ConfigService.get(key, default)` 命中时**忽略**默认值，spec stub 改成按 key 返回映射（之前总返 `'a'.repeat(32)` 导致 expiresIn fallback 不生效）
  - `users` mock 需加 `findOneByOrFail`（refresh 用）
  - TypeScript 字面量类型推断要包含新字段
  - base64 解码忽略 padding 字符错误，tag 篡改测试改 flip 中间字符

**验证记录**（沙盒环境 2026-06-05）:
- `npm run test:unit` → **54/54 PASS**（11 suite，原 10 + auth.service 8 + crypto 11 = 19 新增）
- `npm run start:dev` → Nest 启动 0 errors, Mapped 4 路由：
  - GET /api/health
  - POST /api/auth/sms/send
  - POST /api/auth/login
  - POST /api/auth/refresh
- `curl /api/auth/sms/send {phone:13700137000}` → 200 + Redis `sms:13700137000=396084`
- `curl /api/auth/login {phone:13700137000, code:396084}` → **200** + 真实 JWT:
  - access: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI5MmFkZjVmYS1hY2QzLTRhYzMtYjY1Mi1iNGY1ODRmMzk5YzMiLCJ0eXBlIjoidXNlciIsImlhdCI6MTc4MDY0NjEzNywiZXhwIjoxNzgwNjQ3OTM3fQ...`（30m exp）
  - refresh: 7d exp, 同样 HS256
  - user: `{id: '92adf5fa-...', nickname: '用户7000', phone_mask: '137****7000', credits: 0}`
- `psql SELECT * FROM users` → 1 行，phone_encrypted 长度 **58 字节**（base64(iv)+'.'+base64(enc)+'.'+base64(tag)）
- `curl /api/auth/refresh {refreshToken:'bogus'}` → **401** `{code:401, message:'refresh token 无效或已过期'}`（异常分支）

---

### ✅ Task 11: 用户资料 + 注销 【DONE】

**Goal**: `GET/PATCH/DELETE /api/user/me` 三接口，软删 + 30 天后定时硬删。

**Pre-reqs**: Task 10

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/user/user.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/user/user.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/user/dto/update-profile.dto.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/user/user.service.spec.ts`

**Steps**:

1. **写失败测试**：mock user repo，断言 PATCH 改 nickname 写 `updated_at`，DELETE 设 `deleted_at` + `delete_at = now() + 30d`。

2. **写 `user.service.ts`**：

```typescript
@Injectable()
export class UserService {
  @InjectRepository(UserEntity) private users: Repository<UserEntity>;

  async getMe(userId: string) {
    const u = await this.users.findOneByOrFail({ id: userId });
    return { id: u.id, nickname: u.nickname, avatar: u.avatar,
             phone_mask: this.crypto.mask(u.phone_enc), credits: u.credits };
  }

  async updateMe(userId: string, dto: UpdateProfileDto) {
    await this.users.update(userId, { ...dto });
    return this.getMe(userId);
  }

  async cancel(userId: string) {
    const deleteAt = new Date(Date.now() + 30 * 86400_000);
    await this.users.update(userId, {
      status: 'pending_delete', delete_at: deleteAt, deleted_at: new Date(),
    });
    return { ok: true, delete_at: deleteAt };
  }
}
```

3. **写 `user.controller.ts`**（用 `@CurrentUser()` 装饰器）：

```typescript
@Controller('user')
@UseGuards(JwtAuthGuard)
export class UserController {
  @Get('me') me(@CurrentUser() u: { id: string }) { return this.svc.getMe(u.id); }
  @Patch('me') update(@CurrentUser() u: { id: string }, @Body() dto: UpdateProfileDto) {
    return this.svc.updateMe(u.id, dto);
  }
  @Delete('me') cancel(@CurrentUser() u: { id: string }) { return this.svc.cancel(u.id); }
}
```

4. **Commit**：`feat(user): get/update/cancel me with 30d hard delete scheduled`

**Test**: （见 plan.md Task 11 Done Criteria）

**Commit**: `feat(user): get/update/cancel me with 30d hard delete scheduled`

**Done criteria**:
- 三接口集成测试通过
- DB 看到 `delete_at` 字段写入未来 30 天

**实际 commit**: `8b10e8f` — `feat(user): 用户资料 CRUD + 30 天软删 + 立即 token 吊销`（9 文件，+362/-19）

**实现差异 vs Runbook 规范**:
- **nickname 长度 1-50**（陈工确认）：`@Length(1, 50)` + `@IsString`，不做敏感词过滤。Patch 60 字 → 400 `昵称长度需在 1-50 字符之间`。
- **立即吊销 token**（陈工确认）：cancel 写 Redis 黑名单 `user:cancelled:<id>` = cancel_ms EX **7d**（覆盖 refresh token 最长寿命）。`JwtAuthGuard.canActivate` 在 passport-jwt 通过后查 Redis，比对 `iat*1000 < cancelMs` → 401 `账号已注销`。
- **JwtAuthGuard 升级**:
  - 注入 `REDIS_CLIENT`，`canActivate` 改 async（在 super 之后查 Redis）
  - `handleRequest` **保持同步**（匹配基类 `IAuthGuard` 签名 — TS 不让 override 成 `Promise<T>`，会报 `Type 'Promise<any>' is not assignable to type 'TUser'`）
  - admin token 跳过吊销 check（仅 user scope）
- **JwtPayload 接口对齐**（**重要 fix**）:
  - Task 10 模板里 `JwtPayload` 用 `sub: string`（raw JWT claim），但 `JwtStrategy.validate` 返 `{id, type, role?}`（**把 sub 改名 id**）
  - 之前 `JwtAuthGuard` 查 `user.sub` 永远 undefined，**黑名单 check 默默失效**
  - 修复：把 `JwtPayload.sub` 改成 `id`（与 JwtStrategy 输出对齐），guard 用 `user.id`
  - 沙盒验证：修复前 cancel 后 GET me 仍 200，修复后 401 `账号已注销`
- **controller guard**: `UserController` 加 `@UseGuards(JwtAuthGuard)`，但 JwtAuthGuard 已通过 APP_GUARD 全局注册（Task 5）。两处叠加 OK（class-level 用 guard 加固 explicit）。
- **30d grace 可配置**: `config.get('user.cancellation_grace_days', 30)`，env 没设走 30 天默认。

**验证记录**（沙盒环境 2026-06-05）:
- `npm run test:unit` → **60/60 PASS**（12 suite：原 11 + user.service 4 + guard 升级 3 = 7 新增；1 旧 guard test 修改）
- `npm run start:dev` → 0 errors, Mapped `/api/user/me {GET,PATCH,DELETE}`
- 完整登录流 → me 流 → cancel 流验证：
  - `POST /api/auth/sms/send {phone:13777776666}` → 200, code=998081
  - `POST /api/auth/login` → JWT + user `{id: UUID, nickname: '用户6666', phone_mask: '137****6666', credits: 0}`
  - `GET /api/user/me` → 200 + profile
  - `PATCH /api/user/me {nickname:'新昵称测试'}` → 200 + nickname 改
  - `PATCH 60 字 nickname` → 400 `昵称长度需在 1-50 字符之间`（ValidationPipe 拦截）
  - `DELETE /api/user/me` → 200 + `delete_at: '2026-07-05T...'`
  - Redis `user:cancelled:UUID` = cancel_ms, EX 7d
  - DB `users.status=pending_delete, deleted_at, delete_at=+30d`
  - `GET /api/user/me` AFTER cancel → **401 `账号已注销`**（黑名单吊销生效）

---

### ✅ Task 12: 协议模块 【DONE】 (commit `9a507a2`)

**Goal**: `GET /api/agreement/current?type=` 读 `system_configs.protocols[type].content`，`POST /api/agreement/accept` 写 `user_agreements`，启动时强制弹窗（前端在 Login 页判断）。

**Pre-reqs**: Task 4（表已建）、Task 11（user）

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/agreement/agreement.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/agreement/agreement.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/agreement/agreement.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/migrations/1700000002000-SeedSystemConfigs.ts`（写 `protocols.user.content` 等）

**Steps**:

1. **写失败测试**：mock SystemConfig repo，断言：
   - 已知 type → 返回 content
   - POST accept → 写一条 user_agreements，包含 user_id + type + version
   - 重复接受同 version → 幂等

2. **写 `agreement.service.ts`**：

```typescript
@Injectable()
export class AgreementService {
  async getCurrent(type: 'user' | 'privacy') {
    const cfg = await this.configs.findOneByOrFail({ key: `protocols.${type}` });
    return { type, version: cfg.value.version, content: cfg.value.content };
  }

  async accept(userId: string, type: string, version: string) {
    await this.userAgreements
      .createQueryBuilder()
      .insert()
      .values({ user_id: userId, type, version })
      .orIgnore()  // UNIQUE 约束
      .execute();
    return { ok: true };
  }
}
```

3. **写 migration**（种默认协议 + 模型 + 限流 + 提示词前后缀）：

```typescript
// migrations/1700000002000-SeedSystemConfigs.ts
export class SeedSystemConfigs1700000002000 implements MigrationInterface {
  async up(qr: QueryRunner) {
    await qr.query(`INSERT INTO system_configs (key, value, updated_by) VALUES
      ('protocols.user', '{"version":"v1.0","content":"...待陈工填..."}','system'),
      ('protocols.privacy', '{"version":"v1.0","content":"...待陈工填..."}','system'),
      ('credit_pricing_table', '{"1":2,"2":3,"3":4,"4+":5}','system'),
      ('rate_limit', '{"per_minute":5,"per_day":50,"burst":3}','system'),
      ('ai_models', '{"primary":{"vendor":"tongyi","model":"wanx-v1","enabled":true},
                      "secondary":{"vendor":"hunyuan","model":"hunyuan-vision","enabled":false},
                      "allow_fallback":true}','system'),
      ('prompt_prefix','medical aesthetic reference photo, frontal view, high detail, ','system'),
      ('prompt_suffix',', preserve ethnicity and facial identity, natural result, no text overlay, realistic photograph','system')
    ON CONFLICT (key) DO NOTHING;`);
  }
}
```

4. **Commit**：`feat(agreement): current/accept + seed system configs`

**Test**: （见 plan.md Task 12 Done Criteria）

**Commit**: `feat(agreement): current/accept + seed system configs` → `9a507a2`

**Done criteria**:
- 调 `GET /api/agreement/current?type=user` 返回种子的内容 ✅
- accept 写库 ✅
- 启动 `app` 时 `protocols.user` 已存在 ✅ (种在 1700000000001-SeedPresetsAndConfigs, v1.0)

**实现差异**:
- 种子已在 1700000000001 完成 (jsonb_build_object('v', 'v1.0', 'content', '...')), 不需要新 migration
- 端点: `GET /api/agreement/current` (@Public), `POST /api/agreement/accept` (@Auth)
- version 始终从 system_configs 读 (POST body 只有 `type`), 防客户端伪造旧版
- 双重防御: DTO `@IsIn(['user', 'privacy'])` (400) + service 内部 `PROTOCOL_KEYS[type]` 缺失 (404)
- entity 字段用 camelCase (`userId`) 给 QueryBuilder.values(), TypeORM 翻译到列 `user_id`
- idempotency: UNIQUE(user_id, type, version) + `orIgnore()` 双重保险
- accept 错误路径: type 未知 → 404, 不写库

**验证**:
- unit: 8 agreement tests pass (68/68 total)
- curl: 5 GET 用例 (200/200/200/400/400), 5 POST 用例 (401/201/201/201/400) 全通过
- DB: 2 行 (user v1.0 + privacy v1.0), 重复 accept 验证 `ON CONFLICT DO NOTHING` 0 行返回

---

### ⏳ Task 13: P1 验收 【pending】

**Goal**: 浏览器能登录、看协议、查/改/注销自己账号（阶段 1 里程碑验收）。

**Pre-reqs**: Tasks 9、10、11、12 全部 ✅

**Files**:
- （仅验收，无文件改动；产物：截图归档 + 验收 commit）

**Steps**:

**Acceptance Checklist**：

- [ ] `POST /api/auth/sms/send` 60s 内第二次 429
- [ ] dev 控制台有验证码
- [ ] `POST /api/auth/login` → 返回 access + refresh + user
- [ ] `GET /api/user/me` 带 token 通过；不带 401
- [ ] `PATCH /api/user/me` 改 nickname 成功
- [ ] `DELETE /api/user/me` → 用户 `status=pending_delete`，`delete_at` 是 +30d
- [ ] `GET /api/agreement/current?type=user` 返回种子内容
- [ ] `POST /api/agreement/accept` 写 `user_agreements`，二次幂等
- [ ] Runbook 9-12 状态全 ⏳ → ✅

**Test**: 跑通 Acceptance Checklist 全部 9 项

**Commit**: `chore(release): p1 milestone`

**Done criteria**:
- 截图归档
- commit `chore(release): p1 milestone`

---
## P2: 积分+支付 【pending】

---

### ✅ Task 14: 积分事务（TDD） 【DONE】 (commit `fb63322`)

**Goal**: `creditledger` 服务提供 `consume` / `recharge` / `refund`，全部用 TypeORM 事务。

**Pre-reqs**: Task 4

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/credit/creditledger.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/credit/creditledger.service.spec.ts`（**TDD 红绿**）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/credit/credit.module.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/credit/dto/consume.dto.ts`

**Steps**:

1. **写失败测试**（先红）：

```typescript
describe('CreditLedgerService', () => {
  it('consume deducts from user credits atomically', async () => {
    await svc.recharge(userId, 100, 'init');
    const result = await svc.consume(userId, 30, 'generate', 'gen_1');
    expect(result.balanceAfter).toBe(70);
    const u = await users.findOneByOrFail({ id: userId });
    expect(u.credits).toBe(70);
  });
  it('consume throws INSUFFICIENT_CREDITS when balance < amount', async () => {
    await svc.recharge(userId, 10, 'init');
    await expect(svc.consume(userId, 30, 'gen', 'g'))
      .rejects.toThrow(/INSUFFICIENT_CREDITS/);
    const u = await users.findOneByOrFail({ id: userId });
    expect(u.credits).toBe(10);  // 未扣
  });
  it('recharge adds to credits + writes ledger', async () => {
    await svc.recharge(userId, 100, 'order_1');
    const rows = await ledger.find({ where: { user_id: userId } });
    expect(rows).toHaveLength(1);
    expect(rows[0].balance_after).toBe(100);
  });
  it('refund creates positive ledger entry', async () => {
    await svc.recharge(userId, 100, 'o1');
    await svc.consume(userId, 30, 'g', 'g1');
    await svc.refund(userId, 30, 'g1', 'reason');
    const u = await users.findOneByOrFail({ id: userId });
    expect(u.credits).toBe(100);
  });
});
```

跑 → **FAIL**（无实现）。

2. **写最小实现**（绿）：

```typescript
@Injectable()
export class CreditLedgerService {
  constructor(
    @InjectRepository(CreditLedgerEntity) private ledger: Repository<CreditLedgerEntity>,
    @InjectRepository(UserEntity) private users: Repository<UserEntity>,
  ) {}

  async consume(userId: string, amount: number, type: string, relatedId: string) {
    if (amount <= 0) throw new BadRequestException('amount must be positive');
    return this.dataSource.transaction(async (em) => {
      const u = await em.findOne(UserEntity, { where: { id: userId }, lock: { mode: 'pessimistic_write' } });
      if (!u || u.credits < amount) {
        throw new BadRequestException({ code: 'INSUFFICIENT_CREDITS', message: '积分不足' });
      }
      u.credits -= amount;
      await em.save(u);
      const entry = em.create(CreditLedgerEntity, {
        user_id: userId, type, amount: -amount, balance_after: u.credits, related_id: relatedId,
      });
      await em.save(entry);
      return { balanceAfter: u.credits };
    });
  }

  async recharge(userId: string, amount: number, relatedId: string) {
    return this.dataSource.transaction(async (em) => {
      const u = await em.findOne(UserEntity, { where: { id: userId }, lock: { mode: 'pessimistic_write' } });
      u.credits += amount;
      await em.save(u);
      await em.save(em.create(CreditLedgerEntity, {
        user_id: userId, type: 'recharge', amount, balance_after: u.credits, related_id: relatedId,
      }));
    });
  }

  async refund(userId: string, amount: number, relatedId: string, note: string) {
    return this.recharge.call(this, userId, amount, relatedId);  // type 覆盖
  }
}
```

跑 → **PASS**。
3. **Commit**：`feat(credit): creditledger service with typeorm transaction (TDD)`

**Test**: (见 plan.md Task 14 Done Criteria)

**Commit**: `feat(credit): creditledger service with typeorm transaction (TDD)` → `fb63322`

**Done criteria**:
- 9 unit tests 全绿 (mock EntityManager, lock 模式 / 错误码 / 顺序 / 事务边界) ✅
- 11/11 real-DB integration checks (`scripts/verify-credit.ts`, 跑真 PG) ✅
  - 事务回滚: 402 INSUFFICIENT_CREDITS 抛错后, user.credits 不变 (查询验证)
  - ledger append-only: 3 行 (recharge 100, consume -30, refund 20)
  - balance_after 反规范化准确

**实现差异**:
- 错误码: 402 + `INSUFFICIENT_CREDITS` (陈工确认, RESTful 语义比 400 准)
- 锁: `pessimistic_write` (陈工确认, 并发低 + 强一致)
- amount 验证: 正整数 (`Number.isInteger(amount) && amount > 0`), 0/负数/小数都 400
- refund 独立 type=`REFUND` (与 RECHARGE 区分, 便于 admin 报表)
- 真实 DB 验证脚本 (`scripts/verify-credit.ts`) 留作以后回归用

---

### ⏳ Task 15: 套餐 + 订单 API 【pending】

**Goal**: `GET /api/credit/packages`、`POST /api/order/create`、`GET /api/order/list`、`GET /api/order/:no`。

**Pre-reqs**: Task 14

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/credit-package/credit-package.controller.ts`（含 service）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/order/order.controller.ts`（含 service）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/order/order.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/order/order.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/migrations/1700000001000-SeedCreditPackages.ts`

**Steps**:

1. **写失败测试**（order service）：
   - `create(userId, packageId, 'wechat')` → 写 `orders` status=pending，返 `order_no` + 调支付（mock）
   - `list(userId)` → 按时间倒序

2. **写 seed**（4 档套餐，design §1.1 D2 提到的 50/220/580/2400 积分）：

```typescript
// migrations/1700000001000-SeedCreditPackages.ts
async up(qr: QueryRunner) {
  await qr.query(`INSERT INTO credit_packages (name, credits, price_cents, bonus_credits, validity_days, is_active, sort_order) VALUES
    ('体验', 50, 500, 0, 90, true, 10),
    ('标准', 220, 2000, 20, 180, true, 20),
    ('专业', 580, 5000, 80, 365, true, 30),
    ('旗舰', 2400, 20000, 400, 365, true, 40)
    ON CONFLICT DO NOTHING;`);
}
```

3. **写 `order.service.ts`**（order_no 用 nanoid 12 位）：

```typescript
@Injectable()
export class OrderService {
  async create(userId: string, packageId: string, method: PaymentMethod) {
    const pkg = await this.pkgs.findOneByOrFail({ id: packageId, is_active: true });
    const order = await this.orders.save(this.orders.create({
      order_no: 'M' + nanoid(12),
      user_id: userId, package_id: packageId,
      credits: pkg.credits + pkg.bonus_credits, amount_cents: pkg.price_cents,
      status: 'pending', payment_method: method,
    }));
    const payInfo = await this.payment.createPayUrl(order);  // Task 16/17 实现
    return { order_no: order.order_no, ...payInfo };
  }

  async list(userId: string, page: number, size: number) { /* 分页 */ }
  async detail(userId: string, orderNo: string) { /* 鉴权 + 返详情 */ }
}
```

4. **Commit**：`feat(order): packages list, order create/list/detail + seed 4 packages`

**Test**: (见 plan.md Task 15 Done Criteria)

**Commit**: `feat(order): packages list, order create/list/detail + seed 4 packages`

**Done criteria**:
- 4 档套餐 seed 成功
- create 返回 order_no + 调支付抛"未实现"（占位）

---

### ⏳ Task 16: 微信 H5 支付 【pending】

**Goal**: 封装 `wechatpay-node-v3`，`createPayUrl` 返 H5 跳转 URL（`https://wx.tenpay.com/...`）。

**Pre-reqs**: Task 15

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/wechat/wechat.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/wechat/wechat.service.spec.ts`（mock SDK）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/payment.module.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/.env.example`（追加 5 个 WECHATPAY_* 变量）

**Steps**:

1. **写失败测试**：mock `wechatpay-node-v3`，断言 `createPayUrl` 返 `h5_url`。
2. 安装：
```bash
cd server && npm install wechatpay-node-v3
```

3. **写 `wechat.service.ts`**（**deferred** 真实环境）：

```typescript
@Injectable()
export class WechatService {
  private pay: any;
  constructor(private cfg: ConfigService) {
    if (cfg.get('NODE_ENV') === 'production') {
      this.pay = new WxPay({
        appid: cfg.get('WECHATPAY_APPID'),
        mchid: cfg.get('WECHATPAY_MCH_ID'),
        publicKey: fs.readFileSync('./apiclient_cert.pem'),  // (deferred)
        privateKey: fs.readFileSync('./apiclient_key.pem'),  // (deferred)
        key: cfg.get('WECHATPAY_API_V3_KEY'),
      });
    }
  }
  async createH5Url(order: OrderEntity) {
    if (process.env.NODE_ENV !== 'production') {
      return { h5_url: `https://wx.tenpay.com/mock?order_no=${order.order_no}` };
    }
    const res = await this.pay.transactions_h5({
      description: `积分套餐-${order.credits}`,
      out_trade_no: order.order_no,
      notify_url: this.cfg.get('WECHATPAY_NOTIFY_URL'),
      amount: { total: order.amount_cents, currency: 'CNY' },
      scene_info: { h5_info: { type: 'Wap', app_name: '医美咨询', app_url: 'https://...' } },
    });
    return { h5_url: res.h5_url };
  }
}
```

4. **Commit**：`feat(payment): wechat h5 wrapper with mock fallback`

**Test**: (见 plan.md Task 16 Done Criteria)

**Commit**: `feat(payment): wechat h5 wrapper with mock fallback`

**Done criteria**:
- dev 调 `createH5Url` 返 mock URL
- 生产环境需真实证书（deferred）

---

### ⏳ Task 17: 支付宝 H5 支付 【pending】

**Goal**: 封装 `alipay-sdk`，手机网站支付。

**Pre-reqs**: Task 15

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/alipay/alipay.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/alipay/alipay.service.spec.ts`

**Steps**:

1. **写失败测试**：mock alipay SDK，断言 `createH5Url` 返支付链接。
2. 安装：`npm install alipay-sdk`
3. **写 `alipay.service.ts`**：

```typescript
@Injectable()
export class AlipayService {
  private sdk: any;
  constructor(private cfg: ConfigService) {
    if (cfg.get('NODE_ENV') === 'production') {
      this.sdk = new AlipaySdk({
        appId: cfg.get('ALIPAY_APP_ID'),
        privateKey: cfg.get('ALIPAY_PRIVATE_KEY'),
        alipayPublicKey: cfg.get('ALIPAY_PUBLIC_KEY'),
      });
    }
  }
  async createH5Url(order: OrderEntity) {
    if (process.env.NODE_ENV !== 'production') {
      return { h5_url: `https://openapi.alipay.com/mock?order_no=${order.order_no}` };
    }
    const res = await this.sdk.exec('alipay.trade.wap.pay', {
      notify_url: this.cfg.get('ALIPAY_NOTIFY_URL'),
      bizContent: {
        out_trade_no: order.order_no,
        total_amount: (order.amount_cents / 100).toFixed(2),
        subject: `积分套餐-${order.credits}`,
        product_code: 'QUICK_WAP_WAY',
      },
    });
    return { h5_url: res };
  }
}
```

4. **Commit**：`feat(payment): alipay h5 wrapper with mock fallback`

**Test**: (见 plan.md Task 17 Done Criteria)

**Commit**: `feat(payment): alipay h5 wrapper with mock fallback`

**Done criteria**:
- 单元测试通过（mock alipay SDK）

---

### ⏳ Task 18: 支付回调 + 对账 cron 【pending】

**Goal**:
- `POST /api/payment/wechat/notify`（**不需鉴权**）：验签 → 查 order → 调 `creditledger.recharge` → 写 `ai_call_logs` 不写，这是订单日志；写 audit
- `POST /api/payment/alipay/notify` 同上
- **对账 cron**：每日 02:00 跑 `dailyReconcile`，扫 `orders.status=pending && paid_at < now-30min` 的，调支付平台查单 API，更新或告警

**Pre-reqs**: Tasks 16、17

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/payment.controller.ts`（notify endpoints）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/payment.service.ts`（回调处理 + 对账）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/payment/payment.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/cron/daily-reconcile.cron.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/cron/cron.module.ts`（`@nestjs/schedule` 注册）

**Steps**:

1. **写失败测试**：mock 通知 payload，断言：
   - 验签失败 → 400
   - 验签通过 + order 存在 + status=pending → 调 `recharge` + status=paid + 返回微信要求 XML
2. **写 `payment.service.ts`**：

```typescript
@Injectable()
export class PaymentService {
  async handleWechatNotify(rawXml: string, headers: any) {
    const verified = this.wechat.verifySign(rawXml, headers);  // 内部验签
    if (!verified) throw new BadRequestException('签名错误');
    const data = await this.wechat.decodeNotify(rawXml);
    const order = await this.orders.findOneByOrFail({ order_no: data.out_trade_no });
    if (order.status === 'paid') return { code: 'SUCCESS', message: 'OK' };  // 幂等
    if (data.trade_state !== 'SUCCESS') {
      return { code: 'SUCCESS', message: '已接收' };
    }
    await this.ledger.recharge(order.user_id, order.credits, order.order_no);
    await this.orders.update(order.id, { status: 'paid', txn_id: data.transaction_id, paid_at: new Date() });
    return { code: 'SUCCESS', message: 'OK' };
  }
  // handleAlipayNotify 同理
}
```

3. **写 cron**：

```typescript
@Cron('0 2 * * *', { name: 'dailyReconcile' })
async reconcile() {
  const stale = await this.orders.find({
    where: { status: 'pending', created_at: LessThan(daysAgo(1)) },
  });
  for (const o of stale) {
    const remote = await this.wechat.queryOrder(o.order_no);
    if (remote.trade_state === 'SUCCESS') {
      await this.handleWechatNotify(/* 构造内部对象 */);
    } else {
      await this.notify.warn(`订单 ${o.order_no} 30min 未支付且未取消`);
    }
  }
}
```

4. **Commit**：`feat(payment): notify handler (wechat/alipay) + daily reconcile cron`

**Test**: (见 plan.md Task 18 Done Criteria)

**Commit**: `feat(payment): notify handler (wechat/alipay) + daily reconcile cron`

**Done criteria**:
- mock 通知 → 调 recharge + 写 audit
- cron 手动触发（`POST /api/admin/cron/run/dailyReconcile`）跑通

---

### ⏳ Task 19: P2 验收 【pending】

**Goal**: P2 阶段（积分+支付）端到端验收。

**Pre-reqs**: Tasks 14、15、16、17、18

**Files**:
- 无新文件（仅 checklist 验收）

**Steps**:

1. **逐项跑验收清单**：

- [ ] `GET /api/credit/packages` 返回 4 档
- [ ] `POST /api/order/create` → 返 order_no + mock h5_url
- [ ] mock 微信/支付宝通知 → 调 `recharge` → 用户积分 += 套餐数
- [ ] `users.credits` 缓存 = `credit_ledger` 最后一条 `balance_after`（一致性）
- [ ] 重复通知幂等（不重复入账）
- [ ] 对账 cron 跑通，扫到 stale 订单告警
- [ ] Runbook 14-18 状态全 ⏳ → ✅

2. **Commit**：`chore(release): p2 milestone`

**Test**: (见 plan.md Task 19 Done Criteria)

**Commit**: `chore(release): p2 milestone`

**Done criteria**:
- 全部 checklist 打勾
- 提交 `chore(release): p2 milestone` 标签

---
## P3: 预设+AI 【pending】

### ⏳ Task 20: 预设项目数据迁移 + API 【pending】

**Goal**: 20 项 preset 写入 `preset_items`；`GET /api/preset/list` 按 category 分组。

**Pre-reqs**: Task 4

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/migrations/1700000000001-SeedPresets.ts`（20 项）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/preset/preset.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/preset/preset.controller.ts`

**Steps**:

1. **写 seed**（v1 的 20 项**一字不改**地迁移到 PG，category=鼻/眼/面/颌/肤，各 4 项）：

```typescript
async up(qr: QueryRunner) {
  const items = [
    { key: 'nose_bridge_lift', category: '鼻部', name: '鼻梁增高', sort_order: 10,
      description: '提升鼻梁高度', default_prompt: 'subtly raise and define the nose bridge, ...' },
    // ... 共 20 项（详见 v1 design §5）
  ];
  for (const it of items) {
    await qr.query(`INSERT INTO preset_items
      (key, category, name, description, default_prompt, credits_cost, is_active, sort_order)
      VALUES ($1,$2,$3,$4,$5,2,true,$6) ON CONFLICT (key) DO NOTHING;`,
      [it.key, it.category, it.name, it.description, it.default_prompt, it.sort_order]);
  }
}
```

> ⭐ 20 项内容与 v1 完全一致，业务未变。

2. **写 `preset.service.ts`**：

```typescript
async list() {
  const all = await this.repo.find({
    where: { is_active: true },
    order: { category: 'ASC', sort_order: 'ASC' },
  });
  return all.reduce((acc, it) => {
    (acc[it.category] ??= []).push(it);
    return acc;
  }, {} as Record<string, PresetItemEntity[]>);
}
```

3. **Commit**：`feat(preset): seed 20 items + grouped list api`

**Test**: (见 plan.md Task 20 Done Criteria)

**Commit**: `feat(preset): seed 20 items + grouped list api`

**Done criteria**:
- 20 项 preset 写入 `preset_items`（按 category 鼻/眼/面/颌/肤 各 4 项）
- `GET /api/preset/list` 按 category 分组返回
- seed 在 `ON CONFLICT (key) DO NOTHING` 保护下可重跑

---

### ⏳ Task 21: AI 提示词拼接器（TDD） 【pending】

**Goal**: `prompt.js` 纯函数，prefix + presets AND + user_text + suffix。

**Pre-reqs**: Task 12（`prompt_prefix` / `prompt_suffix` 已在 system_configs）

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/prompt.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/prompt.test.ts`（**TDD 红绿**）

**Steps**:

1. **写失败测试**（`prompt.test.ts`）：

```typescript
import { buildPrompt } from './prompt';

describe('buildPrompt', () => {
  it('joins presets with AND', () => {
    expect(buildPrompt('p_', ['a','b','c'], null, '_s'))
      .toBe('p_a AND b AND c_s');
  });
  it('inserts user_text with comma when present', () => {
    expect(buildPrompt('p_', ['a'], 'make brighter', '_s'))
      .toBe('p_a, make brighter_s');
  });
  it('handles single preset without AND', () => {
    expect(buildPrompt('p_', ['a'], null, '_s'))
      .toBe('p_a_s');
  });
  it('preserves empty user_text as no insertion', () => {
    expect(buildPrompt('p_', ['a','b'], '', '_s'))
      .toBe('p_a AND b_s');
  });
  it('escapes commas in user_text? (NO, we trust user input)', () => {
    expect(buildPrompt('p_', ['a'], 'thin, sharp nose', '_s'))
      .toBe('p_a, thin, sharp nose_s');
  });
});
```

跑 → **FAIL**。

2. **实现**：

```typescript
// server/src/ai/prompt.ts
export function buildPrompt(
  prefix: string,
  presetPrompts: string[],
  userText: string | null,
  suffix: string,
): string {
  const joined = presetPrompts.length === 1
    ? presetPrompts[0]
    : presetPrompts.join(' AND ');
  const user = userText && userText.trim()
    ? `, ${userText.trim()}` : '';
  return `${prefix}${joined}${user}${suffix}`;
}
```

跑 → **PASS**。

3. **Commit**：`feat(ai): prompt builder with TDD (5 test cases)`

**Done Criteria**：5 个测试全绿。

**Test**: (见 plan.md Task 21 Done Criteria)

**Commit**: `feat(ai): prompt builder with TDD (5 test cases)`

**Done criteria**:
- 5 个 `buildPrompt` 测试用例全绿（单 preset / 多 preset / 含 user_text / 空 user_text / user_text 含逗号）

---

### ⏳ Task 22: AI 适配器（mock + tongyi） 【pending】

**Goal**: `AiAdapter` 接口 + 2 实现：`MockAdapter`（永远成功返 sample 图 URL）+ `TongyiAdapter`（封装通义万相 image editing API）。

**Pre-reqs**: Task 21

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/adapters/ai-adapter.interface.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/adapters/mock.adapter.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/adapters/tongyi.adapter.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/adapters/tongyi.adapter.spec.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/.env.example`（TONGYI_API_KEY）

**Steps**:

1. **写接口**：

```typescript
// server/src/ai/adapters/ai-adapter.interface.ts
export interface AiAdapter {
  name: 'mock' | 'tongyi' | 'hunyuan';
  async editImage(opts: {
    imageSignedUrl: string;
    prompt: string;
  }): Promise<{ resultBuffer: Buffer; modelUsed: string; costCents: number; latencyMs: number }>;
}
```

2. **写 mock**：直接读 `./test/fixtures/sample-face.jpg` 作为结果。

3. **写 tongyi**（**deferred** 真实调用）：

```typescript
@Injectable()
export class TongyiAdapter implements AiAdapter {
  name = 'tongyi' as const;
  async editImage({ imageSignedUrl, prompt }: any) {
    const start = Date.now();
    // (deferred) 真实环境调 dashscope SDK
    if (this.cfg.get('NODE_ENV') !== 'production') {
      return {
        resultBuffer: await fs.promises.readFile('./test/fixtures/sample-face.jpg'),
        modelUsed: 'wanx-v1-mock', costCents: 5, latencyMs: 1000,
      };
    }
    const res = await axios.post('https://dashscope.aliyuncs.com/api/v1/services/aigc/image2image/image-synthesis',
      { model: 'wanx-v1', input: { image_url: imageSignedUrl, prompt },
        parameters: { n: 1 } },
      { headers: { Authorization: `Bearer ${this.cfg.get('TONGYI_API_KEY')}` } });
    const url = res.data.output.results[0].url;
    const buf = await axios.get(url, { responseType: 'arraybuffer' });
    return { resultBuffer: Buffer.from(buf.data), modelUsed: 'wanx-v1',
             costCents: 5, latencyMs: Date.now() - start };
  }
}
```

4. **Commit**：`feat(ai): mock + tongyi adapters with fallback to mock in dev`

**Done Criteria**：mock 单测通过；tongyi dev 环境返 mock，prod 需真 key（deferred）。

**Test**: (见 plan.md Task 22 Done Criteria)

**Commit**: `feat(ai): mock + tongyi adapters with fallback to mock in dev`

**Done criteria**:
- `MockAdapter.editImage` 单元测试通过
- `TongyiAdapter` dev 环境返 mock 结果；prod 需真 `TONGYI_API_KEY`（deferred）
- `.env.example` 增 `TONGYI_API_KEY=`

---

### ⏳ Task 23: hunyuan stub + AIService 入口 【pending】

**Goal**:
- `HunyuanAdapter` 永远抛 `HttpException(501, 'NOT_IMPLEMENTED')`（v2 占位）
- `AIService.generate()`：路由到 primary，失败切 secondary，重试 1 次，**都失败则退积分**

**Pre-reqs**: Task 22

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/adapters/hunyuan.adapter.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/ai.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/ai.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/ai.module.ts`

**Steps**:

1. **写 hunyuan stub**：

```typescript
@Injectable()
export class HunyuanAdapter implements AiAdapter {
  name = 'hunyuan' as const;
  async editImage(): Promise<any> {
    throw new HttpException('NOT_IMPLEMENTED', 501);
  }
}
```

2. **写失败测试**（`ai.service.spec.ts`）：
   - primary 成功 → 不调 secondary
   - primary 5xx → 重试 1 次仍败 → 调 secondary（如允许）
   - 两边都败 → 抛错 + 调 ledger.refund

3. **写 `ai.service.ts`**：

```typescript
@Injectable()
export class AiService {
  async generate(generationId: string, userId: string, imageUrl: string, presetKeys: string[], userText: string | null) {
    const cfg = await this.sysCfg.get('ai_models');  // 含 primary/secondary/allow_fallback
    const { prefix, suffix } = await this.loadPromptAffixes();
    const presets = await this.presetRepo.findBy({ key: In(presetKeys) });
    const prompt = buildPrompt(prefix, presets.map(p => p.default_prompt), userText, suffix);

    const signedUrl = await this.oss.signUrl(imageUrl, 300);
    const errors: any[] = [];
    for (const target of [cfg.primary, cfg.secondary]) {
      if (!target?.enabled) continue;
      const adapter = this.adapters.get(target.vendor);
      for (let attempt = 1; attempt <= 2; attempt++) {
        try {
          return await adapter.editImage({ imageSignedUrl: signedUrl, prompt });
        } catch (e: any) {
          errors.push({ vendor: target.vendor, attempt, msg: e.message });
          if (e.status >= 400 && e.status < 500 && e.status !== 429) break;  // 参数错不重试
        }
      }
    }
    await this.ledger.refund(userId, /* amount */, generationId, 'AI 全部失败');
    await this.generations.update(generationId, { status: 'failed', error_msg: JSON.stringify(errors) });
    throw new InternalServerErrorException({ code: 'AI_FAILED', errors });
  }
}
```

4. **Commit**：`feat(ai): aiservice with hot-swap routing + retry + refund`

**Done Criteria**：5 个测试路径全绿；dev 跑通（mock → 成功）。

**Test**: (见 plan.md Task 23 Done Criteria)

**Commit**: `feat(ai): aiservice with hot-swap routing + retry + refund`

**Done criteria**:
- 5 个 AIService 测试路径全绿（primary 成功 / 5xx 重试 / secondary 接管 / 4xx 不重试 / 全败退积分）
- dev 环境跑通：mock 适配器直接返成功

---

### ⏳ Task 24: Redis 限流 + 站内通知 【pending】

**Goal**:
- 限流：每用户 `per_minute=5`、`per_day=50`、`burst=3`（值从 system_configs 读）
- 通知：建 `notifications` 表已存在；`GET /api/notification/list`（按时间倒序，未读优先）+ `PATCH /:id/read`

**Pre-reqs**: Task 4（notifications 表已建）

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/rate-limiter.ts`（Redis Lua）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/rate-limiter.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/notification/notification.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/notification/notification.controller.ts`

**Steps**:

1. **写限流测试**：
   - 调 5 次 OK，第 6 次返 429
   - 改 `system_configs.rate_limit.per_minute=2` 后，第 3 次 429
2. **写 `rate-limiter.ts`**（Redis Lua 原子计数）：

```lua
-- rate-limit.lua
local per_min = tonumber(ARGV[1])
local cur = redis.call('INCR', KEYS[1])
if cur == 1 then redis.call('EXPIRE', KEYS[1], 60) end
if cur > per_min then return 0 end
return 1
```

```typescript
// server/src/ai/rate-limiter.ts
export async function checkRate(redis: Redis, userId: string, perMin: number) {
  const ok = await redis.eval(LUA, 1, `rl:m:${userId}`, perMin);
  if (ok === 0) throw new HttpException('请求过于频繁', 429);
}
```

3. **写 notification controller**：

```typescript
@Controller('notification')
@UseGuards(JwtAuthGuard)
export class NotificationController {
  @Get('list') list(@CurrentUser() u, @Query() q: ListNotifDto) {
    return this.svc.list(u.id, q);
  }
  @Patch(':id/read')
  read(@CurrentUser() u, @Param('id') id: string) {
    return this.svc.markRead(u.id, id);
  }
}
```

4. **Commit**：`feat(ai): redis lua rate limiter + notification list/read api`

**Done Criteria**：限流单测过；notification list 返分页。

**Test**: (见 plan.md Task 24 Done Criteria)

**Commit**: `feat(ai): redis lua rate limiter + notification list/read api`

**Done criteria**:
- 限流单测过（5 次 OK → 第 6 次 429；改 per_minute=2 后第 3 次 429）
- `GET /api/notification/list` 返分页（按时间倒序，未读优先）
- `PATCH /api/notification/:id/read` 标记已读

---

### ⏳ Task 25: Generate submit（50ms 内）+ Worker 【pending】

**Goal**:
- `POST /api/generate/submit`：JWT → 校验 DTO → 限流 → 算积分 → 事务扣 → 写 generation(pending) → 入 Bull → 50ms 内返 `{generation_id}`
- **Worker 进程**（独立 `src/workers/generate.worker.ts`）：消费队列 → 调 AIService → `sharp` 合成 AI 角标 + 免责水印 → 上传 OSS → 写 generation(success) + notification

**Pre-reqs**: Tasks 14, 21, 22, 23, 24

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/generate/generate.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/generate/generate.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/generate/generate.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/workers/generate.worker.ts`（独立入口）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/ai/image-watermark.ts`（sharp 合成）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/oss/oss.service.ts`
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/app.module.ts`（注册 BullModule）

**Steps**:

1. **写失败测试**（`generate.service.spec.ts`）：mock Bull + ledger + ai service，断言 submit 后 generation 状态=pending 且 ledger 扣了分。

2. **写 `generate.service.ts`**（submit 关键路径）：

```typescript
@Injectable()
export class GenerateService {
  @InjectQueue('ai.generate') private queue: Queue;

  async submit(userId: string, dto: SubmitDto) {
    const cfg = await this.sysCfg.get('credit_pricing_table');
    const cost = this.calcCost(dto.preset_keys.length, cfg);  // 2/3/4/5
    const generation = await this.dataSource.transaction(async (em) => {
      const u = await em.findOne(UserEntity, { where: { id: userId }, lock: { mode: 'pessimistic_write' } });
      if (u.credits < cost) throw new BadRequestException({ code: 'INSUFFICIENT_CREDITS' });
      u.credits -= cost;
      await em.save(u);
      const g = em.create(GenerationEntity, {
        user_id: userId, original_url: dto.image_url, preset_keys: dto.preset_keys,
        prompt_text: '', credits_cost: cost, status: 'pending',
        expires_at: new Date(Date.now() + 30 * 86400_000),
      });
      return em.save(g);
    });
    // 流水账
    await this.ledger.appendConsume(userId, cost, 'generate', generation.id);
    await this.queue.add('edit', { generationId: generation.id, userId, ...dto });
    return { generation_id: generation.id };
  }
}
```

3. **写 worker**（独立进程入口 `src/workers/generate.worker.ts`）：

```typescript
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Job } from 'bullmq';

@Processor('ai.generate')
export class GenerateProcessor extends WorkerHost {
  async process(job: Job) {
    const { generationId, userId, imageUrl, presetKeys, text } = job.data;
    try {
      const result = await this.ai.generate(generationId, userId, imageUrl, presetKeys, text);
      const watermarked = await this.watermark.add(result.resultBuffer);
      const resultUrl = await this.oss.upload(`gen/${generationId}.jpg`, watermarked);
      await this.gensRepo.update(generationId, {
        status: 'success', result_url: resultUrl, model_used: result.modelUsed,
      });
      await this.notif.create(userId, 'generation_done',
        '生成完成', `查看您的 AI 预览图`, { generation_id: generationId });
      await this.aiLogs.logSuccess({ generationId, model: result.modelUsed, cost: result.costCents, latency: result.latencyMs });
    } catch (e) {
      await this.aiLogs.logFailure({ generationId, error: e.message });
      throw e;  // 队列重试
    }
  }
}
```

4. **写 `image-watermark.ts`**（sharp）：

```typescript
@Injectable()
export class WatermarkService {
  async add(buf: Buffer): Promise<Buffer> {
    const img = sharp(buf);
    const { width, height } = await img.metadata();
    const svg = Buffer.from(`
      <svg width="${width}" height="${height}">
        <text x="20" y="${height - 30}" font-size="22" fill="rgba(255,255,255,0.85)" stroke="rgba(0,0,0,0.4)" stroke-width="0.5">
          AI 模拟预览，仅供娱乐参考，不构成医疗建议
        </text>
        <rect x="${width - 110}" y="${height - 60}" width="90" height="40" fill="rgba(212,165,160,0.9)" rx="6"/>
        <text x="${width - 95}" y="${height - 33}" font-size="20" fill="white">AI 生成</text>
      </svg>
    `);
    return img.composite([{ input: svg, top: 0, left: 0 }]).jpeg({ quality: 90 }).toBuffer();
  }
}
```

5. **Commit**：`feat(generate): submit + worker with watermark + oss upload + notification`

**Done Criteria**：
- `POST /api/generate/submit` 50ms 内返 `{generation_id}`
- worker 跑通（dev 走 mock adapter），看到 notifications 表有 1 条
- 失败路径 → ledger 退积分 + generation=failed

**Test**: `time curl -X POST -H "Authorization: Bearer $JWT" -H "Content-Type: application/json" -d '{"image_url":"...","preset_keys":["nose_bridge_lift"]}' http://localhost:3000/api/generate/submit` 50ms 内返回；worker 跑通后 `psql -c 'SELECT * FROM notifications WHERE type=$$generation_done$$ ORDER BY id DESC LIMIT 1'` 见 1 条。

**Commit**: `feat(generate): submit + worker with watermark + oss upload + notification`

**Done criteria**:
- `POST /api/generate/submit` 50ms 内返 `{generation_id}`（`time` 命令验证）
- worker 跑通（dev 走 mock adapter），`notifications` 表新增 1 条 `generation_done`
- 失败路径（mock 抛错）→ ledger 退积分 + `generation.status='failed'`

---

### ⏳ Task 26: Generate 5 辅助接口 【pending】

**Goal**:
- `GET /api/generate/status/:id`（轮询降级用，返 status + result_url if success）
- `GET /api/generate/list`（我的历史，分页 + status 过滤）
- `GET /api/generate/:id`（详情）
- `DELETE /api/generate/:id`（软删，status=deleted）
- `GET /api/generate/:id/download-url`（OSS 临时签名 URL，TTL 5min）

**Pre-reqs**: Task 25

**Files**:
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/generate/generate.controller.ts`（追加 5 端点）
- Modify: `/Users/chenjin/Downloads/beautify_work/server/src/generate/generate.service.ts`（追加方法）

**Steps**:

1. **写失败测试**（每个端点 1 个用例）
2. **写 5 方法**：

```typescript
async status(userId, id) {
  const g = await this.gensRepo.findOne({ where: { id, user_id: userId } });
  return { id: g.id, status: g.status, result_url: g.status === 'success' ? g.result_url : null };
}

async list(userId, page, size, statusFilter) {
  return this.gensRepo.findAndCount({
    where: { user_id: userId, ...(statusFilter && { status: statusFilter }) },
    order: { created_at: 'DESC' }, skip: (page - 1) * size, take: size,
  });
}

async detail(userId, id) { /* + ai_logs 摘要 */ }
async delete(userId, id) { await this.gensRepo.update({ id, user_id: userId }, { status: 'deleted' }); }
async downloadUrl(userId, id) {
  const g = await this.gensRepo.findOneOrFail({ where: { id, user_id: userId } });
  if (g.status !== 'success') throw new BadRequestException('生成未成功');
  const url = await this.oss.signUrl(g.result_url, 300);
  await this.downloadLogs.log(userId, id);  // 合规
  return { url, expires_in: 300 };
}
```

3. **Commit**：`feat(generate): status/list/detail/delete/download-url endpoints`

**Test**: (见 plan.md Task 26 Done Criteria)

**Commit**: `feat(generate): status/list/detail/delete/download-url endpoints`

**Done criteria**:
- 5 个端点（status / list / detail / delete / download-url）单元测试通过
- `download-url` 走 OSS 临时签名（TTL 5min）+ 写 `download_logs`
- `delete` 为软删（`status='deleted'`，不真删记录）

---

### ⏳ Task 27: P3 验收 【pending】

**Acceptance Checklist**:

- [ ] 20 项 preset 写入 DB
- [ ] `GET /api/preset/list` 按 5 类分组
- [ ] 5 个 prompt 测试全绿
- [ ] submit 接口 50ms 内返（用 `time` 命令测）
- [ ] worker 跑通：mock → 角标 + 通知
- [ ] 失败路径：mock 改成 throw → ledger 退 + gen=failed
- [ ] 限流：调 6 次 submit 第 6 次 429
- [ ] 5 辅助接口 200 + 数据正确
- [ ] Runbook 20-26 状态全 ⏳ → ✅

**Done Criteria**：commit `chore(release): p3 milestone`。

**Test**: (见 plan.md Task 27 Done Criteria)

**Commit**: `chore(release): p3 milestone`

**Done criteria**:
- 上述 9 条 Acceptance Checklist 全部勾选
- 提交 `chore(release): p3 milestone`
- Runbook Tasks 20-26 状态全部从 ⏳ 改为 ✅

---
## P4: 咨询师端页面 【pending】

### ⏳ Task 28: 登录 + 工作台 + 我的 + 通知 4 页 【pending】

**Goal**: 4 个核心页面跑通。

**Pre-reqs**: Tasks 6, 10, 11

**Files**（全部在 `/Users/chenjin/Downloads/beautify_work/web/src/pages/`）:
- Create: `Login.vue`（手机号 + 验证码 + 协议勾选 + @click 跳 dashboard）
- Create: `Dashboard.vue`（用户卡片 + 积分余额 + 2 大按钮（去生成/去充值）+ 最近 3 张生成）
- Create: `Profile.vue`（头像昵称 + 积分明细跳历史 + 设置 + 协议 + 注销确认弹窗）
- Create: `Notifications.vue`（list 卡片，未读红点 + 标记已读）

**Steps**:

1. **写 Pinia stores**（`stores/user.ts`、`stores/notification.ts`）
2. **写 Login.vue** 关键逻辑：

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { showToast } from 'vant';
import { useUserStore } from '@/stores/user';
import { sendSms, login } from '@/api/auth';

const phone = ref('');
const code = ref('');
const countdown = ref(0);
const user = useUserStore();
const router = useRouter();

async function onSend() {
  if (countdown.value > 0) return;
  await sendSms(phone.value);
  countdown.value = 60;
  const t = setInterval(() => {
    countdown.value--;
    if (countdown.value <= 0) clearInterval(t);
  }, 1000);
}

async function onLogin() {
  const res = await login(phone.value, code.value);
  user.setToken(res.token);
  user.setRefreshToken(res.refresh_token);
  user.setUser(res.user);
  router.push('/dashboard');
}
</script>
```

3. **写 Dashboard.vue** 关键布局：

```vue
<template>
  <div class="dashboard">
    <van-card :title="user.nickname" :thumb="user.avatar" />
    <div class="credits">余额：{{ user.credits }} 积分</div>
    <van-grid :column-num="2" gutter="10">
      <van-grid-item icon="photo-o" text="去生成" to="/generate" />
      <van-grid-item icon="balance-o" text="去充值" to="/recharge" />
    </van-grid>
    <h3>最近生成</h3>
    <van-grid :column-num="3">
      <van-grid-item v-for="g in recent" :key="g.id">
        <img :src="g.result_url" alt="" />
      </van-grid-item>
    </van-grid>
  </div>
</template>
```

4. **Profile.vue + Notifications.vue** 类似实现（详见 runbook）
5. **Commit**: `feat(web): login + dashboard + profile + notifications pages`

**Test**: (见 plan.md Task 28 Done Criteria)

**Commit**: `feat(web): login + dashboard + profile + notifications pages`

**Done criteria**:
- 浏览器手测四页跳得通、数据正确
- 响应式断点 mixin 在 desktop 上自动隐藏 Vant 的 mobile-only 元素

---

### ⏳ Task 29: 生成 + 生成中 + 结果 3 页 【pending】

**Goal**: 核心业务三步走通。

**Pre-reqs**: Task 28（dashboard 跳得通）、Task 25（submit 接口）

**Files**:
- Create: `Generate.vue`（上传原图 + 多选 preset + 描述 + 实时显示积分价 + 提交）
- Create: `Generating.vue`（loading + 轮询 status + "完成后通知" 提示 + 取消）
- Create: `Result.vue`（左右滑对比 + 下载 + 反馈）

**Steps**:

1. **写 Generate.vue**：

```vue
<template>
  <div>
    <van-uploader v-model="file" :max-count="1" :after-read="upload" />
    <van-checkbox-group v-model="selectedPresets">
      <van-cell-group v-for="(items, cat) in presets" :title="cat" :key="cat">
        <van-checkbox v-for="p in items" :key="p.key" :name="p.key">
          {{ p.name }} <span class="cost">+{{ costOf(p) }}积分</span>
        </van-checkbox>
      </van-cell-group>
    </van-checkbox-group>
    <van-field v-model="text" placeholder="可选：补充描述" type="textarea" rows="3" />
    <div class="price">合计：{{ totalCost }} 积分</div>
    <van-button block :disabled="!file || !selectedPresets.length" @click="submit">
      提交（扣 {{ totalCost }} 积分）
    </van-button>
  </div>
</template>
```

2. **写 Generating.vue**（轮询）：

```vue
<script setup>
const route = useRoute();
const router = useRouter();
const id = route.params.id;
const status = ref('pending');
const timer = ref();

onMounted(() => {
  timer.value = setInterval(async () => {
    const r = await getStatus(id);
    status.value = r.status;
    if (r.status === 'success') { clearInterval(timer.value); router.push(`/result/${id}`); }
    if (r.status === 'failed') { clearInterval(timer.value); showDialog({ title: '生成失败', message: '积分已退还' }); }
  }, 3000);
});
onUnmounted(() => clearInterval(timer.value));
</script>
```

3. **写 Result.vue**：左右滑对比用 `vue-compare-image` 组件；下载调 `getDownloadUrl` 拿 OSS 签名链接。
4. **Commit**: `feat(web): generate + generating + result pages with polling`

**Test**: (见 plan.md Task 29 Done Criteria)

**Commit**: `feat(web): generate + generating + result pages with polling`

**Done criteria**:
- 浏览器手动跑"上传 → 选 preset → 提交 → 等 mock worker 跑完 → 看到结果图带角标"
- 全程 < 1min

---

### ⏳ Task 30: 历史 + 充值 + 订单详情 + 协议 4 页 【pending】

**Goal**: 剩余 4 页。

**Pre-reqs**: Tasks 14, 15, 12

**Files**:
- Create: `History.vue`（缩略图 grid + 状态过滤 + 跳详情）
- Create: `Recharge.vue`（4 档套餐卡片 + 协议勾选 + 调起支付）
- Create: `OrderDetail.vue`（订单号、金额、状态、时间、复制按钮）
- Create: `Agreement.vue`（长文本 + 滚动到底部才能点"同意"）

**Steps**:

1. **写 Recharge.vue**：

```vue
<van-radio-group v-model="pkg">
  <van-cell-group>
    <van-cell v-for="p in packages" :key="p.id" clickable @click="pkg = p.id">
      <template #title>{{ p.name }} ({{ p.credits + p.bonus_credits }}积分)</template>
      <template #right-icon>
        <van-radio :name="p.id" />
        <span class="price">¥{{ (p.price_cents/100).toFixed(2) }}</span>
      </template>
    </van-cell>
  </van-cell-group>
</van-radio-group>
<van-button @click="pay('wechat')">微信支付</van-button>
<van-button @click="pay('alipay')">支付宝</van-button>
```

2. **写 Agreement.vue**（必须滚到底才能点同意）：

```vue
<script setup>
const scrolledToBottom = ref(false);
function onScroll(e) {
  if (e.target.scrollTop + e.target.clientHeight >= e.target.scrollHeight - 5) {
    scrolledToBottom.value = true;
  }
}
</script>
<template>
  <div class="agreement" @scroll="onScroll">
    <div v-html="content" />
  </div>
  <van-button :disabled="!scrolledToBottom" @click="onAccept">同意</van-button>
</template>
```

3. **Commit**: `feat(web): history + recharge + order-detail + agreement pages`

**Test**: (见 plan.md Task 30 Done Criteria)

**Commit**: `feat(web): history + recharge + order-detail + agreement pages`

**Done criteria**:
- 4 个页面均能正常跳转与渲染
- Agreement 页滚到底部后"同意"按钮方可点击

---

### ⏳ Task 31: P4 验收 【pending】

**Goal**: 验收 P4 全部 11 个 Vue 页面，确保完整业务流可跑通。

**Pre-reqs**: Tasks 28, 29, 30

**Files**: (验收任务，无新增文件)

**Steps**:

1. **走完整业务流**：手机号登录 → 充值 50 积分 → 选 1 预设生成 → 看到结果带角标 → 历史列表可见 → 注销账号
2. **检查 11 个页面无 404、无 console error**
3. **响应式断点验证**：在 375px / 768px / 1280px 三个断点测过布局
4. **检查 Vant 4 / Element Plus 组件按需加载**（首屏 < 300KB gzip）
5. **跑 Lighthouse Mobile Performance**，确保 > 80
6. **更新 Runbook 状态**：Runbook 28-30 状态全 ⏳ → ✅
7. **Commit**: `chore(release): p4 milestone`
8. **截图存 `docs/p4-screenshots/`**

**Acceptance Checklist**:
- [ ] 11 个页面无 404、无 console error
- [ ] 浏览器响应式：在 375px / 768px / 1280px 三个断点测过布局
- [ ] 完整业务流：手机号登录 → 充值 50 积分 → 选 1 预设生成 → 看到结果带角标 → 历史列表可见 → 注销账号
- [ ] Vant 4 / Element Plus 组件按需加载（首屏 < 300KB gzip）
- [ ] Lighthouse Mobile Performance > 80
- [ ] Runbook 28-30 状态全 ⏳ → ✅

**Test**: (见 plan.md Task 31 Done Criteria)

**Commit**: `chore(release): p4 milestone`

**Done criteria**:
- commit `chore(release): p4 milestone` 成功
- 截图存 `docs/p4-screenshots/`

---
## P5: 运营后台 【pending】

### ⏳ Task 32: 后台登录 + 鉴权 + 路由守卫 【pending】

**Goal**: admin 登录 + JWT + 路由守卫

**Pre-reqs**: Task 7 ✅、Task 4 ✅（admin_users 表已建）

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/admin/auth/admin-auth.controller.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/admin/auth/admin-auth.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/admin/jwt.strategy.ts`（admin 专用）
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/admin/auth/admin-auth.service.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/stores/auth.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/pages/Login.vue`
- Create: `/Users/chenjin/Downloads/beautify_work/admin/src/api/auth.ts`

**Steps**:

1. **写 admin auth service**（bcrypt 校验 + 签 admin JWT）：

```typescript
@Injectable()
export class AdminAuthService {
  async login(username: string, password: string) {
    const u = await this.repo.findOne({ where: { username } });
    if (!u || !(await bcrypt.compare(password, u.password_hash))) {
      throw new UnauthorizedException('账号或密码错误');
    }
    u.last_login_at = new Date();
    await this.repo.save(u);
    return {
      token: await this.jwt.signAsync(
        { sub: u.id, type: 'admin', role: u.role },
        { expiresIn: '4h' },
      ),
      admin: { id: u.id, username: u.username, role: u.role },
    };
  }
}
```

2. **写 admin Login.vue**（用户名 + 密码 + 强制改密弹窗 if 首次登录）
3. **路由守卫**（Task 7 写过，复用）
4. **seed 默认 admin**：

```sql
INSERT INTO admin_users (username, password_hash, role) VALUES
  ('admin', '$2b$10$...bcrypt of "admin123"', 'super')
ON CONFLICT (username) DO NOTHING;
```

5. **Commit**：`feat(admin): login + jwt + route guard + default admin seed`

**Test**: (见 plan.md Task 32 Done Criteria)

**Commit**: `feat(admin): login + jwt + route guard + default admin seed`

**Done criteria**:
- 浏览器 `localhost:5174/admin/login` 输入 admin/admin123 → 跳 dashboard

---

### ⏳ Task 33: 5 模块（dashboard / orders / users / presets / packages） 【pending】

**Goal**: 基础 5 模块

**Pre-reqs**: Tasks 32, 4, 15

**Files**（按模块拆 controller + page）:
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/dashboard/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Dashboard.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/orders/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Orders.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/users/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Users.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/presets/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Presets.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/packages/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Packages.vue`

**Steps**（每模块相同节奏）:

1. **写后端**（举例 orders）：

```typescript
@Controller('admin/orders')
@UseGuards(AdminAuthGuard, RolesGuard)
export class AdminOrdersController {
  @Get() list(@Query() q: ListOrdersDto) {
    return this.svc.list(q);
  }
  @Get(':id') detail(@Param('id') id: string) {
    return this.svc.detail(id);
  }
}
```

2. **写前端**（用 Element Plus `el-table` + `el-pagination`）：

```vue
<template>
  <el-table :data="rows" stripe>
    <el-table-column prop="order_no" label="订单号" />
    <el-table-column prop="amount_cents" label="金额(分)" />
    <el-table-column prop="status" label="状态" />
    <el-table-column prop="created_at" label="创建时间" />
  </el-table>
  <el-pagination v-model:current-page="page" :page-size="20" :total="total" @current-change="load" />
</template>
```

3. **Dashboard.vue**（4 KPI + 7 日趋势 + 套餐占比 + 待处理）：

```vue
<el-row :gutter="20">
  <el-col v-for="k in kpis" :key="k.label" :span="6">
    <el-card><div class="kpi">{{ k.label }}<div class="num">{{ k.value }}</div></div></el-card>
  </el-col>
</el-row>
<el-chart :option="trendOption" />
```

4. **Commit**：`feat(admin): dashboard/orders/users/presets/packages modules`

**Test**: (见 plan.md Task 33 Done Criteria)

**Commit**: `feat(admin): dashboard/orders/users/presets/packages modules`

**Done criteria**:
- 5 模块可查询、可翻页
- Dashboard 4 KPI 数据正确

---

### ⏳ Task 34: 4 模块（configs / refunds / ai-logs / audit-logs） 【pending】

**Goal**: 剩余 4 模块

**Pre-reqs**: Task 33

**Files**:
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/configs/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Configs.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/refunds/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/Refunds.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/ai-logs/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/AILogs.vue`
- `/Users/chenjin/Downloads/beautify_work/server/src/admin/audit-logs/`、`/Users/chenjin/Downloads/beautify_work/admin/src/pages/AuditLogs.vue`

**Steps**:

1. **Configs.vue**（KV 编辑器，按 key 列表，点击编辑 → JSON 弹窗 → 保存）：

```vue
<el-table :data="configs">
  <el-table-column prop="key" label="Key" />
  <el-table-column label="Value">
    <template #default="{ row }">
      <el-button @click="edit(row)">{{ truncate(row.value) }}</el-button>
    </template>
  </el-table-column>
  <el-table-column prop="updated_at" label="更新时间" />
</el-table>
```

2. **Refunds.vue**（申请列表 + 通过/拒绝 + 调起原路退款 API）：

```typescript
@Post(':id/approve') approve(@Param('id') id: string) {
  return this.svc.approve(id);  // 内部：order → refund API → ledger.refund
}
@Post(':id/reject') reject(@Param('id') id: string, @Body() body: { reason: string }) {
  return this.svc.reject(id, body.reason);
}
```

3. **AILogs.vue**（列表 + 失败明细侧边栏）
4. **AuditLogs.vue**（所有运营操作流水）
5. **Commit**：`feat(admin): configs/refunds/ai-logs/audit-logs modules`

**Test**: (见 plan.md Task 34 Done Criteria)

**Commit**: `feat(admin): configs/refunds/ai-logs/audit-logs modules`

**Done criteria**: (plan.md Task 34 未列具体 Done Criteria，待补充)

---

### ⏳ Task 35: P5 验收 【pending】

**Goal**: P5 阶段验收（运营可上线管理）

**Pre-reqs**: Tasks 32-34

**Files**: 无（验收任务）

**Steps**:

1. **逐项核对 Acceptance Checklist**：
   - admin 登录、token 持久化
   - 9 模块路由可访问，无 404
   - 改 preset → 前端用户端 `GET /api/preset/list` 立即生效（5 分钟内热加载）
   - 改套餐 → 充值页显示新套餐
   - 退款申请 → approve → ledger refund + 状态=approved
   - Runbook 32-34 状态全 ⏳ → ✅

2. **commit**：`chore(release): p5 milestone`

**Test**: (见 plan.md Task 35 Done Criteria)

**Commit**: `chore(release): p5 milestone`

**Done criteria**:
- commit `chore(release): p5 milestone`

---
## P6: 收尾 【pending】

---

### ⏳ Task 36: 监控告警 【pending】

**Goal**: `@willsoto/nestjs-prometheus` 暴露 `/metrics`，Grafana dashboard 4 个面板，企业微信 webhook 4 类告警。

**Pre-reqs**: Task 3

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/metrics/metrics.module.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/metrics/metrics.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/server/src/common/notify/wechat-bot.service.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/prometheus.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/grafana-dashboards/main.json`
- Modify: `/Users/chenjin/Downloads/beautify_work/docker-compose.yml`（加 prometheus + grafana 服务）

**Steps**:

1. **写 metrics module**：

```typescript
@Module({
  imports: [
    PrometheusModule.register({
      path: '/metrics',
      defaultMetrics: { enabled: true },
    }),
  ],
  providers: [MetricsService],
  exports: [MetricsService],
})
export class MetricsModule {}
```

2. **4 个核心指标**：
   - `http_requests_total{method,route,status}`
   - `ai_call_latency_seconds{model,success}`（histogram）
   - `credit_operations_total{type}`（counter）
   - `active_users_24h`（gauge）

3. **写告警规则**（prometheus.yml）：

```yaml
groups:
- name: mae_alerts
  rules:
  - alert: HighErrorRate
    expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
    for: 5m
    annotations: { summary: "5xx 错误率 > 5%" }
  - alert: AiFailureHigh
    expr: rate(ai_call_latency_seconds_count{success="false"}[10m]) > 0.15
    for: 10m
    annotations: { summary: "AI 失败率 > 15%" }
  - alert: CreditLedgerMismatch
    expr: ...
    annotations: { summary: "积分账不平" }
  - alert: DiskSpaceLow
    expr: node_filesystem_avail_bytes / node_filesystem_size_bytes < 0.1
    annotations: { summary: "磁盘空间 < 10%" }
```

4. **写 webhook**：

```typescript
async notify(text: string) {
  const url = this.cfg.get('WECHAT_BOT_WEBHOOK');
  if (!url) return;
  await fetch(url, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ msgtype: 'markdown', markdown: { content: text } }),
  });
}
```

5. **Commit**：`feat(monitor): prometheus + grafana + wechat webhook alerts`

**Test**: `curl http://localhost:3000/metrics` 返 prometheus 格式；4 个告警规则文件就位。

**Commit**: `feat(monitor): prometheus + grafana + wechat webhook alerts`

**Done criteria**:
- `curl http://localhost:3000/metrics` 返 prometheus 格式
- 4 个告警规则文件就位
- Grafana dashboard 4 个面板可见

---

### ⏳ Task 37: E2E 测试 + CI/CD 【pending】

**Goal**:
- **Playwright** 4 主流程 E2E：登录/充值/生成/历史
- **GitHub Actions** CI：lint + test + build + docker push

**Pre-reqs**: Tasks 28, 29, 30

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/tests/e2e/playwright.config.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/tests/e2e/01-login.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/tests/e2e/02-recharge.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/tests/e2e/03-generate.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/tests/e2e/04-history.spec.ts`
- Create: `/Users/chenjin/Downloads/beautify_work/.github/workflows/ci.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/.github/workflows/deploy-prod.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/server.Dockerfile`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/worker.Dockerfile`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/web.Dockerfile`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/admin.Dockerfile`

**Steps**:

1. **装 Playwright**：

```bash
npm install --save-dev @playwright/test
npx playwright install --with-deps chromium
```

2. **写 E2E 01-login.spec.ts**：

```typescript
import { test, expect } from '@playwright/test';
test('user can login via phone code', async ({ page, request }) => {
  // 1. 触发发短信，从 dev 日志里拿 code（用 fetch 调 mock）
  await request.post('/api/auth/sms/send', { data: { phone: '13800138000' } });
  const code = await getLatestDevCode();  // 从 dev server 日志
  // 2. 浏览器登录
  await page.goto('/login');
  await page.fill('[data-test=phone]', '13800138000');
  await page.fill('[data-test=code]', code);
  await page.click('[data-test=submit]');
  await expect(page).toHaveURL(/\/dashboard/);
});
```

3. **03-generate.spec.ts**（重点）：走通"上传 → 选 1 预设 → 提交 → 轮询 → 看到 result_url"

4. **写 CI**（`.github/workflows/ci.yml`）：

```yaml
name: CI
on: [push, pull_request]
jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    services:
      postgres: { image: postgres:16, env: { POSTGRES_USER: mae, POSTGRES_PASSWORD: pwd, POSTGRES_DB: mae_test },
                   ports: ['5432:5432'], options: --health-cmd pg_isready --health-interval 5s }
      redis: { image: redis:7, ports: ['6379:6379'] }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'npm' }
      - run: cp server/.env.example server/.env
      - run: npm ci
      - run: npx typeorm migration:run -d migrations/data-source.ts
      - run: npm run lint
      - run: npm test
      - run: npx playwright install --with-deps chromium
      - run: npm run e2e
      - run: npm run build  # server + web + admin 三套 build
```

5. **写 deploy-prod.yml**（**deferred** 真实镜像推送 + ECS 部署）：

```yaml
name: Deploy Prod
on:
  push: { branches: [main], paths: ['server/**', 'web/**', 'admin/**'] }
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aliyun/acr-login@v1
        with: { registry: ${{ secrets.ACR_REGISTRY }}, username: ${{ secrets.ACR_USER }} }
      - run: docker build -f docker/server.Dockerfile -t $REGISTRY/mae-server:${{ github.sha }} .
      - run: docker push $REGISTRY/mae-server:${{ github.sha }}
      # (deferred) SSH 到 ECS 拉镜像重启
```

6. **Commit**：
- `test(e2e): playwright 4 main flows`
- `ci: github actions lint+test+build+docker-push`

**Test**: 本地 `npx playwright test` 4 个 spec 全绿；CI 跑通；deferred 项（真实推送）有 TODO 注释。

**Commit**:
- `test(e2e): playwright 4 main flows`
- `ci: github actions lint+test+build+docker-push`

**Done criteria**:
- 本地 `npx playwright test` 4 个 spec 全绿
- CI 跑通
- deferred 项（真实推送）有 TODO 注释

---

### ⏳ Task 38: 部署 + 发布 【pending】

**Goal**: 生产 docker-compose + nginx 反代 + 备份策略 + v2.0 GA。

**Pre-reqs**: Task 37

**Files**:
- Create: `/Users/chenjin/Downloads/beautify_work/docker-compose.prod.yml`
- Create: `/Users/chenjin/Downloads/beautify_work/docker/nginx.conf`
- Create: `/Users/chenjin/Downloads/beautify_work/scripts/backup-pg.sh`
- Create: `/Users/chenjin/Downloads/beautify_work/scripts/restore-pg.sh`
- Modify: `/Users/chenjin/Downloads/beautify_work/README.md`（更新到 v2.0 文案）

**Steps**:

1. **写 `docker-compose.prod.yml`**（6 服务）：

```yaml
version: '3.9'
services:
  web:
    image: $REGISTRY/mae-web:$TAG
    restart: always
  admin:
    image: $REGISTRY/mae-admin:$TAG
    restart: always
  server:
    image: $REGISTRY/mae-server:$TAG
    env_file: .env.prod
    restart: always
    depends_on: [postgres, redis]
  worker:
    image: $REGISTRY/mae-worker:$TAG
    command: node dist/workers/generate.worker.js
    env_file: .env.prod
    restart: always
    depends_on: [postgres, redis]
  postgres: { image: postgres:16-alpine, volumes: ['pgdata:/var/lib/postgresql/data'], restart: always }
  redis: { image: redis:7-alpine, restart: always }
  nginx:
    image: nginx:alpine
    ports: ['80:80', '443:443']
    volumes: ['./docker/nginx.conf:/etc/nginx/nginx.conf', './certs:/etc/nginx/certs']
    depends_on: [web, admin, server]
volumes: { pgdata: {} }
```

2. **写 `nginx.conf`**（关键路由）：

```nginx
server {
  listen 443 ssl;
  server_name h5.mae.example.com;
  # ...ssl cert
  location / { root /usr/share/nginx/web; try_files $uri $uri/ /index.html; }
  location /api/ { proxy_pass http://server:3000; proxy_set_header Authorization $http_authorization; }
  location /socket.io/ { proxy_pass http://server:3000; }
}
server {
  listen 443 ssl;
  server_name admin.mae.example.com;
  location / { root /usr/share/nginx/admin; try_files $uri $uri/ /index.html; }
  location /api/ { proxy_pass http://server:3000; }
}
```

3. **写 `backup-pg.sh`**（每日 cron）：

```bash
#!/bin/bash
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
docker exec mae_pg pg_dump -U mae -d mae_prod | gzip > /backup/mae_${TIMESTAMP}.sql.gz
# 保留 7 天
find /backup -name "mae_*.sql.gz" -mtime +7 -delete
# (deferred) 上传 OSS
```

4. **写 v2.0 release runbook**（陈工执行）：

```bash
# 1. (deferred) SSH 到 ECS
ssh ecs-user@prod-server
# 2. (deferred) 拉新镜像
docker-compose -f docker-compose.prod.yml pull
# 3. (deferred) 跑 migration
docker-compose -f docker-compose.prod.yml run --rm server npx typeorm migration:run
# 4. 重启
docker-compose -f docker-compose.prod.yml up -d
# 5. 冒烟测试
curl https://h5.mae.example.com/api/health
# 6. (deferred) 灰度 10% 流量（用 nginx upstream weight）
# 7. 全量
```

5. **更新 README.md**：

```markdown
# 医美咨询 AI 预览 H5 网页

> 医美咨询师专用工具，上传客户照片 + AI 生成整形预览，按次积分计费。

## 技术栈

- 前端：Vue 3 + Vite + Vant 4（用户端）/ Element Plus（运营后台）
- 后端：NestJS 11 + TypeORM + PostgreSQL 16 + Redis 7 + Bull 队列
- 部署：Docker Compose + 阿里云 ECS

## 开发

\`\`\`bash
docker-compose up -d
cd server && npm run start:dev
cd web && npm run dev
cd admin && npm run dev
\`\`\`

## 部署

详见 `docs/deploy.md` 与 GitHub Actions workflow。
```

6. **Commit**：`docs: v2.0 release notes + deploy scripts + backup`

**Test**:
- `docker-compose -f docker-compose.prod.yml config` 通过
- `nginx -t -c docker/nginx.conf` 通过
- `./scripts/backup-pg.sh` 手动跑一次成功

**Commit**: `docs: v2.0 release notes + deploy scripts + backup`

**Done criteria**:
- `docker-compose.prod.yml` 语法 `docker-compose config` 通过
- `nginx.conf` 语法 `nginx -t` 通过
- backup script 手动跑一次成功
- README 更新
- Runbook 所有 38 任务 ✅

---

---

---

## 3. 卡壳怎么办？

| 症状 | 排查 |
|---|---|
| `docker compose up -d` 失败 | Docker Desktop 未启 / 5432/6379 端口被占 → `lsof -i :5432` / `lsof -i :6379` |
| `npm install` 失败 | Node 版本（需 20+，用 `nvm use`） / 网络（用 pnpm 更快）|
| `npm run test:unit` 失败 | 看 traceback；先确认测试是不是已 import 实际代码 |
| `migration:run` 失败 | 检查 `dataSource` 配置 / `entities` 路径 / DB 是否启动 |
| TypeORM 找不到实体 | 检查 `entities: [__dirname + '/**/*.entity{.ts,.js}']` glob 模式 |
| 短信发送失败 | 阿里云 AccessKey / 签名 / 模板号 / 余额 |
| 支付回调收不到 | 回调 URL 必须公网可达（开发用 ngrok/钉钉内网穿透）|
| AI 调用失败 | API key / 配额 / 临时签名 URL 过期（OSS 签名默认 30 min）|
| Bull 队列堆积 | Redis 内存 / Worker 进程是否在跑 / 任务失败被 retry |
| Token 失效 | access 过期调 /api/auth/refresh；refresh 过期重新登录 |
| DB 写入失败 | 配额 / 字段类型不匹配 / 唯一索引冲突 |
| 前端响应式错位 | 检查 `<768px` 断点；Vant 组件默认就是移动端，PC 端用 Element Plus |
| 跨域问题 | NestJS 配 `app.enableCors({ origin: [FRONTEND_URL, ADMIN_URL] })` |
| Docker 镜像太大 | 多阶段构建；server 用 `node:20-alpine`；sharp 用官方镜像 |

---

## 4. 持续执行的建议节奏

- **每天 1-2 个任务**，每个任务完成后打 ✅ + 更新 Runbook
- 遇到 **(deferred)** 的步骤跳过即可，不阻塞下一个任务
- 阶段结束（Task 8/13/19/27/31/35）时停下来 review + 跑全部测试 + 打 tag
- 跑测试清单：

```bash
# 每天开工前
cd /Users/chenjin/Downloads/beautify_work
git status
git log --oneline -5

# 每个 Task 完成后
npm run test:unit

# 集成测试（需 docker compose up）
npm run test:integration

# 阶段验收
git tag p0-foundation  # Task 8
git tag p1-user-agreement  # Task 13
git tag p2-credit-payment  # Task 19
git tag p3-ai-integration  # Task 27
git tag p4-frontend  # Task 31
git tag p5-admin  # Task 35
git tag v2.0.0  # Task 38
```

---

## 5. 相关文档索引

| 文档 | 路径 |
|---|---|
| 设计文档 | `../specs/2026-06-04-医美咨询H5-design.md` |
| 实施计划 | `../plans/2026-06-04-医美咨询H5-implementation.md` |
| 工作草稿（已废弃）| `../../医美咨询小程序-设计草稿-2026-06-04.md` |
| v1 Runbook（已废弃）| `./2026-06-04-医美咨询小程序-runbook.md` |
| 本 Runbook | `2026-06-04-医美咨询H5-runbook.md` |

---

*Runbook v2.0 — H5 + NestJS + PostgreSQL, 重写于 2026-06-05*
